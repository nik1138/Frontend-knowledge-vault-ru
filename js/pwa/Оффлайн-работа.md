---
aliases: ["Оффлайн-режим", "Offline Mode", "Работа без интернета"]
tags: ["#pwa", "#offline", "#caching", "#service-workers", "#javascript"]
---

# Оффлайн-работа

Оффлайн-работа - одна из ключевых возможностей прогрессивных веб-приложений (PWA), позволяющая пользователям продолжать использовать приложение даже при отсутствии интернет-соединения. В 2025 году в условиях российских реалий, где качество интернет-соединения может варьироваться, оффлайн-функциональность становится особенно важной.

## Основы оффлайн-работы

Оффлайн-режим в PWA реализуется с помощью комбинации:

- [[Service Workers]] - для перехвата сетевых запросов
- [[Кэширование]] - для хранения ресурсов и данных
- Web App Manifest - для установки приложения

## Архитектура оффлайн-приложений

### Статический контент

Статические ресурсы (HTML, CSS, JavaScript, изображения) кэшируются при установке Service Worker и доступны в оффлайн-режиме:

```javascript
const CACHE_NAME = 'offline-v1';
const STATIC_ASSETS = [
  '/',
  '/index.html',
  '/styles/main.css',
  '/scripts/app.js',
  '/images/logo.png',
  '/offline.html'
];

self.addEventListener('install', function(event) {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then(function(cache) {
        return cache.addAll(STATIC_ASSETS);
      })
  );
});
```

### Динамический контент

Для работы с динамическими данными (например, API-ответами) используется стратегия кэширования с последующей синхронизацией:

```javascript
self.addEventListener('fetch', function(event) {
  if (event.request.url.includes('/api/')) {
    event.respondWith(
      fetch(event.request)
        .then(function(response) {
          // Кэшируем успешный ответ
          var responseClone = response.clone();
          caches.open('api-cache-v1')
            .then(function(cache) {
              cache.put(event.request, responseClone);
            });
          return response;
        })
        .catch(function() {
          // Возвращаем кэшированный ответ при оффлайн-режиме
          return caches.match(event.request)
            .then(function(cachedResponse) {
              if (cachedResponse) {
                return cachedResponse;
              }
              // Возвращаем заглушку для оффлайн-режима
              return caches.match('/offline-data.json');
            });
        })
    );
  }
});
```

## Стратегии оффлайн-работы

### 1. Оффлайн-первый подход

Приложение сначала пытается использовать локальные данные, а затем синхронизирует изменения при восстановлении соединения:

```javascript
class OfflineFirstStorage {
  constructor() {
    this.dbName = 'pwa-offline-db';
    this.version = 1;
    this.db = null;
  }

  async init() {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(this.dbName, this.version);
      
      request.onerror = () => reject(request.error);
      request.onsuccess = () => {
        this.db = request.result;
        resolve();
      };
      
      request.onupgradeneeded = (event) => {
        const db = event.target.result;
        const store = db.createObjectStore('data', { keyPath: 'id' });
        store.createIndex('timestamp', 'timestamp', { unique: false });
      };
    });
  }

  async saveData(data) {
    const transaction = this.db.transaction(['data'], 'readwrite');
    const store = transaction.objectStore('data');
    return store.put(data);
  }

  async getData(id) {
    const transaction = this.db.transaction(['data'], 'readonly');
    const store = transaction.objectStore('data');
    return store.get(id);
  }

  async getAllData() {
    const transaction = this.db.transaction(['data'], 'readonly');
    const store = transaction.objectStore('data');
    return store.getAll();
  }
}
```

### 2. Стратегия синхронизации

При восстановлении интернет-соединения приложение синхронизирует локальные изменения с сервером:

```javascript
// Регистрация Background Sync
self.addEventListener('sync', function(event) {
  if (event.tag === 'sync-data') {
    event.waitUntil(syncData());
  }
});

function syncData() {
  return new Promise((resolve, reject) => {
    // Получаем локальные данные для синхронизации
    getLocalChanges()
      .then(function(changes) {
        return Promise.all(changes.map(function(change) {
          return fetch('/api/sync', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify(change)
          });
        }));
      })
      .then(function(responses) {
        // Удаляем синхронизированные изменения из локального хранилища
        return clearLocalChanges();
      })
      .then(resolve)
      .catch(reject);
  });
}

// Использование Background Sync при потере соединения
async function saveWithSync(data) {
  // Сохраняем данные локально
  await saveLocalData(data);
  
  // Запрашиваем синхронизацию при восстановлении соединения
  if ('serviceWorker' in navigator && 'sync' in navigator.serviceWorker) {
    try {
      await navigator.serviceWorker.ready;
      await navigator.serviceWorker.sync.register('sync-data');
    } catch (error) {
      console.error('Background Sync не поддерживается:', error);
    }
  }
}
```

## Обработка оффлайн-событий

### Определение состояния соединения

```javascript
// Проверка состояния соединения
function isOnline() {
  return navigator.onLine;
}

// Слушатели событий изменения соединения
window.addEventListener('online', function() {
  console.log('Соединение восстановлено');
  // Запускаем синхронизацию
  syncPendingChanges();
});

window.addEventListener('offline', function() {
  console.log('Соединение потеряно');
  // Показываем уведомление пользователю
  showOfflineNotification();
});
```

### Индикация оффлайн-режима

```javascript
// Показ индикатора оффлайн-режима
function showOfflineNotification() {
  const notification = document.createElement('div');
  notification.className = 'offline-notification';
  notification.innerHTML = `
    <div class="offline-banner">
      <span>⚠️</span> Режим оффлайн
      <button onclick="this.parentElement.remove()">Закрыть</button>
    </div>
  `;
  document.body.appendChild(notification);
}

// Обновление интерфейса при изменении состояния
function updateOnlineStatus() {
  const onlineIndicator = document.getElementById('online-status');
  if (onlineIndicator) {
    if (navigator.onLine) {
      onlineIndicator.textContent = '🌐 Онлайн';
      onlineIndicator.className = 'online-status online';
    } else {
      onlineIndicator.textContent = '🔴 Оффлайн';
      onlineIndicator.className = 'online-status offline';
    }
  }
}

// Инициализация индикатора
document.addEventListener('DOMContentLoaded', function() {
  updateOnlineStatus();
  
  window.addEventListener('online', updateOnlineStatus);
  window.addEventListener('offline', updateOnlineStatus);
});
```

## Оффлайн-страницы

### Создание оффлайн-страницы

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Оффлайн - Мое приложение</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 50px;
      background-color: #f5f5f5;
    }
    
    .offline-container {
      max-width: 500px;
      margin: 0 auto;
      background: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }
    
    .offline-icon {
      font-size: 64px;
      margin-bottom: 20px;
    }
    
    .retry-button {
      background-color: #2196F3;
      color: white;
      border: none;
      padding: 12px 24px;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
    }
    
    .retry-button:hover {
      background-color: #0b7dda;
    }
  </style>
</head>
<body>
  <div class="offline-container">
    <div class="offline-icon">📶</div>
    <h1>Вы в оффлайн-режиме</h1>
    <p>Похоже, у вас нет подключения к интернету. Приложение работает в автономном режиме.</p>
    <p>Некоторые функции могут быть недоступны до восстановления соединения.</p>
    <button class="retry-button" onclick="location.reload()">Проверить соединение</button>
  </div>

  <script>
    // Проверка соединения и автоматическое обновление при восстановлении
    window.addEventListener('online', function() {
      location.reload();
    });
  </script>
</body>
</html>
```

### Использование оффлайн-страницы в Service Worker

```javascript
self.addEventListener('fetch', function(event) {
  // Обработка навигационных запросов (страниц)
  if (event.request.mode === 'navigate') {
    event.respondWith(
      fetch(event.request)
        .catch(function() {
          // Возвращаем оффлайн-страницу при ошибках
          return caches.match('/offline.html');
        })
    );
  }
});
```

## Хранение данных в оффлайн-режиме

### IndexedDB для сложных данных

```javascript
class OfflineDataManager {
  constructor() {
    this.dbName = 'pwa-data';
    this.version = 1;
    this.db = null;
  }

  async init() {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(this.dbName, this.version);
      
      request.onerror = () => reject(request.error);
      request.onsuccess = () => {
        this.db = request.result;
        resolve();
      };
      
      request.onupgradeneeded = (event) => {
        const db = event.target.result;
        
        // Создаем хранилища для разных типов данных
        if (!db.objectStoreNames.contains('posts')) {
          const postStore = db.createObjectStore('posts', { keyPath: 'id' });
          postStore.createIndex('date', 'date', { unique: false });
        }
        
        if (!db.objectStoreNames.contains('users')) {
          const userStore = db.createObjectStore('users', { keyPath: 'id' });
          userStore.createIndex('email', 'email', { unique: true });
        }
      };
    });
  }

  async savePost(post) {
    const transaction = this.db.transaction(['posts'], 'readwrite');
    const store = transaction.objectStore('posts');
    return store.put(post);
  }

  async getPost(id) {
    const transaction = this.db.transaction(['posts'], 'readonly');
    const store = transaction.objectStore('posts');
    return store.get(id);
  }

  async getAllPosts() {
    const transaction = this.db.transaction(['posts'], 'readonly');
    const store = transaction.objectStore('posts');
    return store.getAll();
  }

  async saveUser(user) {
    const transaction = this.db.transaction(['users'], 'readwrite');
    const store = transaction.objectStore('users');
    return store.put(user);
  }
}

// Использование менеджера оффлайн-данных
const offlineManager = new OfflineDataManager();

document.addEventListener('DOMContentLoaded', async function() {
  await offlineManager.init();
  
  // Загрузка данных из локального хранилища
  const posts = await offlineManager.getAllPosts();
  renderPosts(posts);
});
```

### LocalStorage для простых данных

```javascript
class SimpleOfflineStorage {
  static set(key, value) {
    try {
      localStorage.setItem(key, JSON.stringify(value));
      return true;
    } catch (e) {
      console.error('Не удалось сохранить данные в localStorage:', e);
      return false;
    }
  }

  static get(key, defaultValue = null) {
    try {
      const item = localStorage.getItem(key);
      return item ? JSON.parse(item) : defaultValue;
    } catch (e) {
      console.error('Не удалось получить данные из localStorage:', e);
      return defaultValue;
    }
  }

  static remove(key) {
    localStorage.removeItem(key);
  }

  static clear() {
    localStorage.clear();
  }
}

// Использование для хранения настроек
function saveUserPreferences(preferences) {
  SimpleOfflineStorage.set('user-preferences', preferences);
}

function getUserPreferences() {
  return SimpleOfflineStorage.get('user-preferences', {
    theme: 'light',
    language: 'ru',
    notifications: true
  });
}
```

## Практические рекомендации для российских разработчиков

В 2025 году при реализации оффлайн-функциональности в России важно учитывать:

### 1. Особенности подключения

- Переменное качество интернета в разных регионах
- Популярность мобильного интернета
- Ограничения на традиционные магазины приложений

### 2. Региональные особенности

- Нестабильное соединение в удаленных регионах
- Высокая стоимость мобильного трафика в некоторых регионах
- Популярность бюджетных устройств с ограниченной памятью

### 3. Рекомендации по реализации

```javascript
// Адаптивная стратегия кэширования в зависимости от качества соединения
function getCacheStrategy() {
  if ('connection' in navigator) {
    const connection = navigator.connection;
    
    // При медленном соединении используем больше кэширования
    if (connection.effectiveType === 'slow-2g' || connection.effectiveType === '2g') {
      return 'cache-first';
    } else if (connection.effectiveType === '3g') {
      return 'stale-while-revalidate';
    }
  }
  
  // По умолчанию - сеть первая
  return 'network-first';
}

// Использование в Service Worker
self.addEventListener('fetch', function(event) {
  const strategy = getCacheStrategy();
  
  if (strategy === 'cache-first') {
    event.respondWith(
      caches.match(event.request)
        .then(function(response) {
          return response || fetch(event.request);
        })
    );
  } else if (strategy === 'network-first') {
    event.respondWith(
      fetch(event.request)
        .then(function(response) {
          var responseClone = response.clone();
          caches.open('dynamic-v1')
            .then(function(cache) {
              cache.put(event.request, responseClone);
            });
          return response;
        })
        .catch(function() {
          return caches.match(event.request);
        })
    );
  }
});
```

### 4. Локализация оффлайн-опыта

```javascript
// Поддержка русскоязычных сообщений об оффлайн-режиме
const offlineMessages = {
  ru: {
    title: 'Оффлайн режим',
    message: 'Вы в оффлайн-режиме. Некоторые функции могут быть недоступны.',
    retry: 'Проверить соединение',
    sync: 'Синхронизировать данные'
  },
  en: {
    title: 'Offline Mode',
    message: 'You are currently offline. Some features may be unavailable.',
    retry: 'Check connection',
    sync: 'Sync data'
  }
};

function getOfflineMessage(lang = 'ru') {
  const messages = offlineMessages[lang] || offlineMessages.ru;
  return messages;
}

// Использование в интерфейсе
function showOfflineMessage() {
  const lang = document.documentElement.lang || 'ru';
  const messages = getOfflineMessage(lang);
  
  // Показываем сообщение с учетом языка пользователя
  const notification = document.createElement('div');
  notification.className = 'offline-notification';
  notification.innerHTML = `
    <div class="offline-banner">
      <strong>${messages.title}</strong>
      <p>${messages.message}</p>
      <button onclick="location.reload()">${messages.retry}</button>
    </div>
  `;
  
  document.body.appendChild(notification);
}
```

## Тестирование оффлайн-функциональности

### Использование DevTools

1. Откройте Chrome DevTools (F12)
2. Перейдите на вкладку Application
3. Выберите раздел Service Workers
4. Включите "Offline" для симуляции отсутствия соединения
5. Проверьте работу приложения

### Автоматизированное тестирование

```javascript
// Пример теста оффлайн-функциональности
describe('Оффлайн функциональность', function() {
  beforeEach(function() {
    // Симуляция оффлайн-режима
    Object.defineProperty(navigator, 'onLine', {
      writable: true,
      value: false
    });
  });

  afterEach(function() {
    // Восстановление онлайн-режима
    Object.defineProperty(navigator, 'onLine', {
      writable: true,
      value: true
    });
  });

  it('должна отображать оффлайн-страницу при отсутствии соединения', function() {
    // Тестирование отображения оффлайн-страницы
    return fetch('/')
      .then(response => {
        assert.equal(response.status, 200);
        // Проверяем, что возвращается оффлайн-страница
      });
  });

  it('должна использовать кэшированные данные', function() {
    // Тестирование использования кэшированных данных
    return caches.open('static-v1')
      .then(cache => {
        return cache.match('/styles/main.css');
      })
      .then(response => {
        assert.isDefined(response);
      });
  });
});
```

## Совместимость и ограничения

Оффлайн-функциональность работает во всех современных браузерах:
- Chrome: с версии 40 (Service Workers)
- Firefox: с версии 44 (Service Workers)
- Safari: с версии 11.1 (ограниченная поддержка)
- Edge: с версии 17

> [!warning] Важно
> Оффлайн-функциональность требует HTTPS для работы Service Workers. Даже на локальных серверах необходимо использовать HTTPS.

## Лучшие практики

1. **Кэшируйте критические ресурсы** - статические файлы, необходимые для базовой работы приложения
2. **Используйте стратегию "сеть первая"** для актуальных данных
3. **Обеспечьте плавный переход** между онлайн и оффлайн режимами
4. **Предупреждайте пользователя** о переходе в оффлайн-режим
5. **Реализуйте фоновую синхронизацию** при восстановлении соединения

## Связанные темы

- [[Service Workers]]
- [[Кэширование]]
- [[Web-App-Manifest]]
- [[Современные практики JavaScript]]
- [[Производительность веб-приложений]]
- [[Браузерные API]]