# Архитектура Доступности

Архитектура доступности — это совокупность принципов, паттернов и практик, определяющих создание веб-приложений, которые могут эффективно использоваться людьми с различными возможностями и ограничениями. Хорошо спроектированная архитектура доступности обеспечивает равный доступ к информации и функциональности для всех пользователей, независимо от их физических или когнитивных способностей.

## Что такое Доступность

Доступность (accessibility) — это практика проектирования и разработки продуктов, устройств, услуг или среды для людей с ограниченными возможностями. В контексте веб-разработки доступность означает создание веб-сайтов и приложений, которые могут быть восприняты, поняты, использованы и взаимодействованы всеми людьми, включая тех, кто использует вспомогательные технологии.

## Принципы Доступности

### 1. Воспринимаемость (Perceivable)

Информация и пользовательские интерфейсы должны быть представлены пользователям в форматах, которые они могут воспринять.

#### Альтернативный Текст для Изображений
```html
<!-- Хорошо - содержательный alt текст -->
<img src="chart.png" alt="График продаж за 2023 год. Рост на 15% по сравнению с 2022 годом">

<!-- Хорошо - декоративное изображение -->
<img src="decoration.png" alt="">

<!-- Хорошо - информативное изображение -->
<img src="printer-icon.png" alt="Печать документа">

<!-- Плохо - отсутствует alt текст -->
<img src="chart.png">

<!-- Плохо - бесполезный alt текст -->
<img src="chart.png" alt="график">
```

#### Аудио и Видео Контент
```html
<!-- Видео с субтитрами и описанием -->
<video controls>
  <source src="presentation.mp4" type="video/mp4">
  <track kind="captions" src="captions.vtt" srclang="ru" label="Русские субтитры">
  <track kind="descriptions" src="descriptions.vtt" srclang="ru" label="Аудио описание">
  Ваш браузер не поддерживает видео.
</video>

<!-- Аудио с транскрипцией -->
<audio controls>
  <source src="podcast.mp3" type="audio/mpeg">
  Ваш браузер не поддерживает аудио.
</audio>
<a href="transcript.html">Транскрипция подкаста</a>
```

### 2. Управляемость (Operable)

Пользовательские интерфейсы и навигация должны быть управляемы различными способами.

#### Клавиатурная Навигация
```javascript
// Компонент с поддержкой клавиатурной навигации
class AccessibleButton {
  constructor(element) {
    this.element = element;
    this.init();
  }
  
  init() {
    // Обработка клика мышью
    this.element.addEventListener('click', this.handleClick.bind(this));
    
    // Обработка клавиатуры
    this.element.addEventListener('keydown', this.handleKeydown.bind(this));
    
    // Установка нужных атрибутов
    this.element.setAttribute('tabindex', '0');
    this.element.setAttribute('role', 'button');
  }
  
  handleClick(event) {
    this.performAction();
  }
  
  handleKeydown(event) {
    // Пробел и Enter активируют кнопку
    if (event.key === ' ' || event.key === 'Enter') {
      event.preventDefault();
      this.performAction();
    }
    
    // Стрелки для навигации (если это часть группы)
    if (this.isInGroup) {
      this.handleArrowKeys(event);
    }
  }
  
  performAction() {
    // Выполнение действия кнопки
    this.element.dispatchEvent(new CustomEvent('action'));
  }
  
  handleArrowKeys(event) {
    switch (event.key) {
      case 'ArrowLeft':
        this.focusPrevious();
        break;
      case 'ArrowRight':
        this.focusNext();
        break;
      case 'ArrowUp':
        this.focusUp();
        break;
      case 'ArrowDown':
        this.focusDown();
        break;
      default:
        return;
    }
    
    event.preventDefault();
  }
}
```

#### Управление Фокусом
```javascript
// Система управления фокусом
class FocusManager {
  constructor() {
    this.focusableElements = [];
    this.currentIndex = -1;
  }
  
  // Получение всех элементов, которые могут получать фокус
  getFocusableElements(container) {
    return container.querySelectorAll(
      'a[href], button, textarea, input[type="text"], input[type="radio"], input[type="checkbox"], select, [tabindex]:not([tabindex="-1"])'
    );
  }
  
  // Установка фокуса на следующий элемент
  focusNext() {
    if (this.focusableElements.length === 0) return;
    
    this.currentIndex = (this.currentIndex + 1) % this.focusableElements.length;
    this.focusableElements[this.currentIndex].focus();
  }
  
  // Установка фокуса на предыдущий элемент
  focusPrevious() {
    if (this.focusableElements.length === 0) return;
    
    this.currentIndex = (this.currentIndex - 1 + this.focusableElements.length) % this.focusableElements.length;
    this.focusableElements[this.currentIndex].focus();
  }
  
  // Установка фокуса на первый элемент
  focusFirst() {
    if (this.focusableElements.length === 0) return;
    
    this.currentIndex = 0;
    this.focusableElements[this.currentIndex].focus();
  }
  
  // Установка фокуса на последний элемент
  focusLast() {
    if (this.focusableElements.length === 0) return;
    
    this.currentIndex = this.focusableElements.length - 1;
    this.focusableElements[this.currentIndex].focus();
  }
  
  // Ловушка фокуса для модальных окон
  trapFocus(container) {
    const focusableElements = this.getFocusableElements(container);
    const firstElement = focusableElements[0];
    const lastElement = focusableElements[focusableElements.length - 1];
    
    const trapHandler = (event) => {
      if (event.key === 'Tab') {
        if (event.shiftKey && document.activeElement === firstElement) {
          event.preventDefault();
          lastElement.focus();
        } else if (!event.shiftKey && document.activeElement === lastElement) {
          event.preventDefault();
          firstElement.focus();
        }
      }
    };
    
    container.addEventListener('keydown', trapHandler);
    
    return () => {
      container.removeEventListener('keydown', trapHandler);
    };
  }
}
```

### 3. Понятность (Understandable)

Информация и управление интерфейсом должны быть понятны пользователям.

#### Четкие Метки и Инструкции
```html
<!-- Хорошо - четкая метка -->
<label for="email">Email адрес</label>
<input type="email" id="email" name="email" required>

<!-- Хорошо - группа связанных элементов -->
<fieldset>
  <legend>Выберите ваш пол</legend>
  <input type="radio" id="male" name="gender" value="male">
  <label for="male">Мужской</label>
  <input type="radio" id="female" name="gender" value="female">
  <label for="female">Женский</label>
</fieldset>

<!-- Хорошо - ошибка с пояснением -->
<label for="password">Пароль</label>
<input type="password" id="password" name="password" aria-describedby="password-error" required>
<div id="password-error" class="error-message">
  Пароль должен содержать минимум 8 символов, включая заглавные и строчные буквы, цифры и специальные символы.
</div>
```

#### Предсказуемое Поведение
```javascript
// Предсказуемая навигация
class NavigationManager {
  constructor() {
    this.navigationHistory = [];
  }
  
  // Сохранение истории навигации
  pushState(url, title) {
    this.navigationHistory.push({
      url: url,
      title: title,
      timestamp: Date.now()
    });
    
    // Обновление URL без перезагрузки страницы
    history.pushState({ url: url }, title, url);
  }
  
  // Навигация назад
  goBack() {
    if (this.navigationHistory.length > 1) {
      this.navigationHistory.pop(); // Удаляем текущую страницу
      const previous = this.navigationHistory[this.navigationHistory.length - 1];
      history.back();
    } else {
      // Если история пуста, переходим на главную
      window.location.href = '/';
    }
  }
  
  // Обработка кнопки "Назад" браузера
  handlePopState(callback) {
    window.addEventListener('popstate', (event) => {
      if (this.navigationHistory.length > 1) {
        this.navigationHistory.pop();
        callback(event.state?.url || window.location.pathname);
      } else {
        window.location.href = '/';
      }
    });
  }
}
```

### 4. Надежность (Robust)

Содержание должно быть достаточно надежным, чтобы его могли интерпретировать различные пользовательские агенты, включая вспомогательные технологии.

#### Семантическая Разметка
```html
<!-- Хорошо - правильная семантика -->
<header>
  <h1>Заголовок сайта</h1>
  <nav aria-label="Основная навигация">
    <ul>
      <li><a href="/">Главная</a></li>
      <li><a href="/about">О нас</a></li>
      <li><a href="/contact">Контакты</a></li>
    </ul>
  </nav>
</header>

<main>
  <article>
    <h2>Заголовок статьи</h2>
    <p>Содержание статьи...</p>
  </article>
</main>

<aside aria-label="Дополнительная информация">
  <h3>Похожие статьи</h3>
  <ul>
    <li><a href="/related1">Связанная статья 1</a></li>
    <li><a href="/related2">Связанная статья 2</a></li>
  </ul>
</aside>

<footer>
  <p>&copy; 2023 Мой сайт</p>
</footer>
```

#### ARIA Атрибуты
```html
<!-- Роли и состояния -->
<div role="alert" aria-live="assertive">
  Произошла ошибка при сохранении данных.
</div>

<!-- Скрытие декоративных элементов -->
<span aria-hidden="true" class="icon">★</span>

<!-- Описание сложных компонентов -->
<div role="tablist" aria-label="Настройки">
  <button role="tab" aria-selected="true" aria-controls="panel1">Профиль</button>
  <button role="tab" aria-selected="false" aria-controls="panel2">Безопасность</button>
</div>

<div id="panel1" role="tabpanel" aria-labelledby="tab1">
  <!-- Содержимое вкладки профиля -->
</div>

<div id="panel2" role="tabpanel" aria-labelledby="tab2" hidden>
  <!-- Содержимое вкладки безопасности -->
</div>
```

## Архитектурные Паттерны Доступности

### 1. Progressive Enhancement

Постепенное улучшение функциональности от базового к расширенному.

```html
<!-- Базовая функциональность -->
<button id="like-button">Нравится</button>

<script>
  // Улучшение с JavaScript
  document.getElementById('like-button').addEventListener('click', function() {
    // Асинхронное обновление
    fetch('/api/like', { method: 'POST' })
      .then(response => response.json())
      .then(data => {
        // Обновление UI
        this.classList.add('liked');
        this.setAttribute('aria-pressed', 'true');
        this.textContent = `Нравится (${data.count})`;
      })
      .catch(error => {
        // Обработка ошибки доступно для всех
        const errorMessage = document.createElement('div');
        errorMessage.setAttribute('role', 'alert');
        errorMessage.textContent = 'Не удалось поставить лайк. Попробуйте еще раз.';
        this.parentNode.insertBefore(errorMessage, this.nextSibling);
      });
  });
</script>
```

### 2. Graceful Degradation

Обеспечение базовой функциональности при отсутствии определенных возможностей.

```javascript
// Проверка поддержки функций
class FeatureDetector {
  static supportsServiceWorkers() {
    return 'serviceWorker' in navigator;
  }
  
  static supportsWebGL() {
    try {
      const canvas = document.createElement('canvas');
      return !!(window.WebGLRenderingContext && 
                (canvas.getContext('webgl') || canvas.getContext('experimental-webgl')));
    } catch (e) {
      return false;
    }
  }
  
  static supportsTouch() {
    return 'ontouchstart' in window || navigator.maxTouchPoints > 0;
  }
  
  static supportsARIA() {
    return typeof document.createElement('div').setAttribute === 'function';
  }
}

// Адаптивная реализация
class AccessibleComponent {
  constructor(element) {
    this.element = element;
    this.init();
  }
  
  init() {
    if (FeatureDetector.supportsARIA()) {
      // Используем ARIA для улучшенной доступности
      this.enhanceWithARIA();
    } else {
      // Обеспечиваем базовую функциональность
      this.basicImplementation();
    }
    
    if (FeatureDetector.supportsTouch()) {
      // Добавляем тач-специфичные улучшения
      this.addTouchSupport();
    }
  }
  
  enhanceWithARIA() {
    this.element.setAttribute('role', 'button');
    this.element.setAttribute('tabindex', '0');
    this.element.setAttribute('aria-expanded', 'false');
    
    this.element.addEventListener('click', () => {
      const expanded = this.element.getAttribute('aria-expanded') === 'true';
      this.element.setAttribute('aria-expanded', !expanded);
    });
    
    this.element.addEventListener('keydown', (event) => {
      if (event.key === 'Enter' || event.key === ' ') {
        event.preventDefault();
        this.element.click();
      }
    });
  }
  
  basicImplementation() {
    // Базовая реализация без ARIA
    this.element.addEventListener('click', this.handleClick.bind(this));
    this.element.addEventListener('keydown', (event) => {
      if (event.key === 'Enter') {
        event.preventDefault();
        this.handleClick();
      }
    });
  }
  
  addTouchSupport() {
    // Добавляем поддержку тач-жестов
    let touchStartY = 0;
    
    this.element.addEventListener('touchstart', (event) => {
      touchStartY = event.touches[0].clientY;
    });
    
    this.element.addEventListener('touchend', (event) => {
      const touchEndY = event.changedTouches[0].clientY;
      const swipeDistance = touchStartY - touchEndY;
      
      if (Math.abs(swipeDistance) > 50) {
        // Свайп вверх или вниз
        this.handleSwipe(swipeDistance > 0 ? 'down' : 'up');
      }
    });
  }
  
  handleClick() {
    // Обработка клика
  }
  
  handleSwipe(direction) {
    // Обработка свайпа
  }
}
```

### 3. Inclusive Design Patterns

Паттерны, учитывающие разнообразие пользователей.

```html
<!-- Адаптивная форма с учетом различных потребностей -->
<form id="registration-form">
  <div class="form-group">
    <label for="username">Имя пользователя *</label>
    <input 
      type="text" 
      id="username" 
      name="username" 
      required
      aria-describedby="username-help"
      autocomplete="username"
    >
    <div id="username-help" class="help-text">
      Имя пользователя должно содержать от 3 до 20 символов. Можно использовать буквы, цифры и подчеркивание.
    </div>
  </div>
  
  <div class="form-group">
    <label for="email">Email *</label>
    <input 
      type="email" 
      id="email" 
      name="email" 
      required
      aria-describedby="email-help"
      autocomplete="email"
    >
    <div id="email-help" class="help-text">
      Мы будем использовать этот email для подтверждения аккаунта и восстановления пароля.
    </div>
  </div>
  
  <div class="form-group">
    <label for="password">Пароль *</label>
    <div class="password-container">
      <input 
        type="password" 
        id="password" 
        name="password" 
        required
        aria-describedby="password-requirements"
        autocomplete="new-password"
      >
      <button 
        type="button" 
        id="toggle-password"
        aria-label="Показать пароль"
        aria-pressed="false"
      >
        👁️
      </button>
    </div>
    <div id="password-requirements" class="help-text">
      Пароль должен содержать минимум 8 символов, включая заглавные и строчные буквы, цифры и специальные символы.
    </div>
  </div>
  
  <button type="submit">Зарегистрироваться</button>
</form>

<script>
// Обработка показа/скрытия пароля
document.getElementById('toggle-password').addEventListener('click', function() {
  const passwordInput = document.getElementById('password');
  const isPressed = this.getAttribute('aria-pressed') === 'true';
  
  if (isPressed) {
    passwordInput.type = 'password';
    this.setAttribute('aria-pressed', 'false');
    this.setAttribute('aria-label', 'Показать пароль');
  } else {
    passwordInput.type = 'text';
    this.setAttribute('aria-pressed', 'true');
    this.setAttribute('aria-label', 'Скрыть пароль');
  }
});

// Валидация формы с учетом доступности
document.getElementById('registration-form').addEventListener('submit', function(event) {
  event.preventDefault();
  
  // Проверка валидности
  if (this.checkValidity()) {
    // Отправка формы
    submitForm(new FormData(this));
  } else {
    // Фокус на первом невалидном поле
    const firstInvalid = this.querySelector(':invalid');
    if (firstInvalid) {
      firstInvalid.focus();
      
      // Добавляем сообщение об ошибке
      const errorContainer = document.createElement('div');
      errorContainer.setAttribute('role', 'alert');
      errorContainer.setAttribute('aria-live', 'polite');
      errorContainer.className = 'form-error';
      errorContainer.textContent = 'Пожалуйста, исправьте ошибки в форме.';
      
      // Вставляем перед формой
      this.parentNode.insertBefore(errorContainer, this);
      
      // Удаляем сообщение через 5 секунд
      setTimeout(() => {
        if (errorContainer.parentNode) {
          errorContainer.parentNode.removeChild(errorContainer);
        }
      }, 5000);
    }
  }
});
</script>
```

## Тестирование Доступности

### 1. Автоматическое Тестирование

```javascript
// Инструмент для автоматического тестирования доступности
class AccessibilityTester {
  constructor() {
    this.rules = {
      'missing-alt': this.checkMissingAlt,
      'missing-label': this.checkMissingLabel,
      'low-contrast': this.checkContrast,
      'missing-viewport': this.checkViewport,
      'missing-title': this.checkTitle
    };
  }
  
  async runTests(element = document.body) {
    const results = [];
    
    for (const [ruleName, ruleFunction] of Object.entries(this.rules)) {
      try {
        const violations = await ruleFunction.call(this, element);
        results.push({
          rule: ruleName,
          violations: violations,
          passed: violations.length === 0
        });
      } catch (error) {
        results.push({
          rule: ruleName,
          error: error.message,
          passed: false
        });
      }
    }
    
    return results;
  }
  
  checkMissingAlt(element) {
    const images = element.querySelectorAll('img');
    const violations = [];
    
    images.forEach(img => {
      if (!img.hasAttribute('alt')) {
        violations.push({
          element: img,
          message: 'Изображение не имеет атрибута alt'
        });
      }
    });
    
    return violations;
  }
  
  checkMissingLabel(element) {
    const inputs = element.querySelectorAll('input, textarea, select');
    const violations = [];
    
    inputs.forEach(input => {
      if (input.type !== 'hidden' && !this.hasLabel(input)) {
        violations.push({
          element: input,
          message: 'Поле ввода не имеет связанной метки'
        });
      }
    });
    
    return violations;
  }
  
  hasLabel(input) {
    // Проверяем наличие связанной метки
    const id = input.getAttribute('id');
    if (id && document.querySelector(`label[for="${id}"]`)) {
      return true;
    }
    
    // Проверяем наличие оберточной метки
    if (input.closest('label')) {
      return true;
    }
    
    // Проверяем наличие aria-label
    if (input.hasAttribute('aria-label')) {
      return true;
    }
    
    // Проверяем наличие aria-labelledby
    if (input.hasAttribute('aria-labelledby')) {
      return true;
    }
    
    return false;
  }
  
  checkContrast(element) {
    // Упрощенная проверка контраста
    // В реальности нужно использовать библиотеки типа axe-core
    return [];
  }
  
  checkViewport(element) {
    const viewport = document.querySelector('meta[name="viewport"]');
    if (!viewport) {
      return [{
        element: document.head,
        message: 'Отсутствует метатег viewport'
      }];
    }
    return [];
  }
  
  checkTitle(element) {
    const title = document.querySelector('title');
    if (!title || !title.textContent.trim()) {
      return [{
        element: document.head,
        message: 'Отсутствует заголовок страницы'
      }];
    }
    return [];
  }
}

// Использование
const tester = new AccessibilityTester();
tester.runTests().then(results => {
  console.log('Результаты тестирования доступности:', results);
  
  // Отправляем в систему мониторинга
  sendToAnalytics('accessibility-test', results);
});
```

### 2. Ручное Тестирование

```javascript
// Инструмент для ручного тестирования доступности
class ManualAccessibilityTester {
  constructor() {
    this.checklist = [
      {
        id: 'keyboard-navigation',
        title: 'Навигация с клавиатуры',
        description: 'Все интерактивные элементы доступны с клавиатуры',
        test: this.testKeyboardNavigation
      },
      {
        id: 'screen-reader',
        title: 'Чтение с экрана',
        description: 'Содержимое правильно читается скринридерами',
        test: this.testScreenReader
      },
      {
        id: 'focus-indicators',
        title: 'Индикаторы фокуса',
        description: 'Все элементы с фокусом имеют видимые индикаторы',
        test: this.testFocusIndicators
      },
      {
        id: 'color-contrast',
        title: 'Контраст цветов',
        description: 'Текст имеет достаточный контраст для чтения',
        test: this.testColorContrast
      }
    ];
  }
  
  async runChecklist() {
    const results = [];
    
    for (const item of this.checklist) {
      try {
        const passed = await item.test.call(this);
        results.push({
          id: item.id,
          title: item.title,
          passed: passed,
          description: item.description
        });
      } catch (error) {
        results.push({
          id: item.id,
          title: item.title,
          passed: false,
          description: item.description,
          error: error.message
        });
      }
    }
    
    return results;
  }
  
  testKeyboardNavigation() {
    return new Promise((resolve) => {
      // Инструкция для тестировщика
      alert('Пожалуйста, протестируйте навигацию с клавиатуры:\n' +
            '1. Нажмите Tab для перехода между элементами\n' +
            '2. Используйте Enter/Пробел для активации\n' +
            '3. Используйте стрелки для навигации в группах\n' +
            '4. Проверьте, что все интерактивные элементы доступны\n\n' +
            'Нажмите OK если тест пройден успешно, или Cancel если есть проблемы');
      
      const passed = confirm('Тест навигации с клавиатуры пройден?');
      resolve(passed);
    });
  }
  
  testScreenReader() {
    return new Promise((resolve) => {
      alert('Пожалуйста, протестируйте с использованием скринридера:\n' +
            '1. Включите скринридер (NVDA, JAWS, VoiceOver)\n' +
            '2. Пройдитесь по странице\n' +
            '3. Проверьте, что информация читается правильно\n' +
            '4. Проверьте навигацию по заголовкам\n\n' +
            'Нажмите OK если тест пройден успешно, или Cancel если есть проблемы');
      
      const passed = confirm('Тест со скринридером пройден?');
      resolve(passed);
    });
  }
  
  testFocusIndicators() {
    return new Promise((resolve) => {
      alert('Пожалуйста, проверьте индикаторы фокуса:\n' +
            '1. Перейдите по элементам с помощью Tab\n' +
            '2. Убедитесь, что каждый элемент с фокусом имеет видимую обводку\n' +
            '3. Проверьте, что индикаторы соответствуют стандартам\n\n' +
            'Нажмите OK если тест пройден успешно, или Cancel если есть проблемы');
      
      const passed = confirm('Тест индикаторов фокуса пройден?');
      resolve(passed);
    });
  }
  
  testColorContrast() {
    return new Promise((resolve) => {
      alert('Пожалуйста, проверьте контраст цветов:\n' +
            '1. Используйте инструмент проверки контраста\n' +
            '2. Проверьте контраст основного текста (минимум 4.5:1)\n' +
            '3. Проверьте контраст крупного текста (минимум 3:1)\n' +
            '4. Проверьте контраст интерактивных элементов\n\n' +
            'Нажмите OK если тест пройден успешно, или Cancel если есть проблемы');
      
      const passed = confirm('Тест контраста цветов пройден?');
      resolve(passed);
    });
  }
}

// Использование
const manualTester = new ManualAccessibilityTester();
manualTester.runChecklist().then(results => {
  console.log('Результаты ручного тестирования:', results);
  
  // Генерируем отчет
  generateAccessibilityReport(results);
});
```

## Инструменты и Библиотеки

### 1. Автоматические Инструменты

#### axe-core
```javascript
// Интеграция axe-core для тестирования доступности
import axe from 'axe-core';

class AxeAccessibilityTester {
  async runAnalysis(context = document, options = {}) {
    try {
      const results = await axe.run(context, options);
      
      // Фильтруем только нарушения
      const violations = results.violations;
      
      // Логируем нарушения
      violations.forEach(violation => {
        console.groupCollapsed(
          `Нарушение: ${violation.help} (${violation.id})`
        );
        
        console.log('Описание:', violation.description);
        console.log('Влияние:', violation.impact);
        console.log('Страница с документацией:', violation.helpUrl);
        
        violation.nodes.forEach(node => {
          console.log('Элемент:', node.html);
          console.log('Целевой селектор:', node.target);
          console.log('Ошибки:', node.failureSummary);
        });
        
        console.groupEnd();
      });
      
      return {
        violations: violations,
        passes: results.passes,
        incomplete: results.incomplete,
        timestamp: new Date().toISOString()
      };
    } catch (error) {
      console.error('Ошибка при запуске axe-core:', error);
      throw error;
    }
  }
  
  // Запуск теста при разработке
  async runDevTest() {
    if (process.env.NODE_ENV === 'development') {
      const results = await this.runAnalysis();
      
      if (results.violations.length > 0) {
        console.warn(
          `⚠️  Найдено ${results.violations.length} нарушений доступности`
        );
      } else {
        console.log('✅ Все тесты доступности пройдены');
      }
    }
  }
}

// Использование в тестах
const axeTester = new AxeAccessibilityTester();

// В Jest тестах
describe('Accessibility', () => {
  test('should not have accessibility violations', async () => {
    document.body.innerHTML = `
      <button>Click me</button>
      <img src="test.jpg">
    `;
    
    const results = await axeTester.runAnalysis();
    expect(results.violations).toHaveLength(0);
  });
});
```

#### Lighthouse
```javascript
// Интеграция Lighthouse для аудита доступности
import lighthouse from 'lighthouse';
import chromeLauncher from 'chrome-launcher';

class LighthouseAccessibilityAuditor {
  async runAudit(url, options = {}) {
    const chrome = await chromeLauncher.launch({ chromeFlags: ['--headless'] });
    
    const options = {
      logLevel: 'info',
      output: 'json',
      onlyCategories: ['accessibility'],
      port: chrome.port
    };
    
    const runnerResult = await lighthouse(url, options);
    
    await chrome.kill();
    
    // Извлекаем результаты аудита доступности
    const accessibilityAudit = runnerResult.lhr.categories.accessibility;
    
    return {
      score: accessibilityAudit.score,
      audits: accessibilityAudit.auditRefs.map(ref => ({
        id: ref.id,
        title: runnerResult.lhr.audits[ref.id].title,
        description: runnerResult.lhr.audits[ref.id].description,
        score: runnerResult.lhr.audits[ref.id].score,
        details: runnerResult.lhr.audits[ref.id].details
      }))
    };
  }
  
  // Генерация отчета
  generateReport(auditResults) {
    const report = {
      timestamp: new Date().toISOString(),
      overallScore: auditResults.score,
      passedAudits: auditResults.audits.filter(audit => audit.score === 1),
      failedAudits: auditResults.audits.filter(audit => audit.score < 1),
      summary: {
        total: auditResults.audits.length,
        passed: auditResults.audits.filter(audit => audit.score === 1).length,
        failed: auditResults.audits.filter(audit => audit.score < 1).length
      }
    };
    
    return report;
  }
}

// Использование в CI/CD
const auditor = new LighthouseAccessibilityAuditor();

// В скрипте сборки
async function runAccessibilityAudit() {
  try {
    const results = await auditor.runAudit('http://localhost:3000');
    const report = auditor.generateReport(results);
    
    console.log('Accessibility Audit Report:', report);
    
    // Проверяем минимальный порог
    if (report.overallScore < 0.9) {
      console.error('Accessibility score below threshold (90%)');
      process.exit(1);
    }
  } catch (error) {
    console.error('Accessibility audit failed:', error);
    process.exit(1);
  }
}
```

## Лучшие Практики

### 1. Проектирование с Учетом Доступности

```html
<!-- Дизайн с учетом доступности -->
<style>
/* Достаточный контраст */
.text-primary {
  color: #212121; /* Контраст 12.6:1 на белом фоне */
}

.text-secondary {
  color: #666666; /* Контраст 4.5:1 на белом фоне */
}

/* Видимые индикаторы фокуса */
*:focus {
  outline: 2px solid #005fcc;
  outline-offset: 2px;
}

/* Адаптивные размеры для тач-устройств */
.touch-target {
  min-height: 44px; /* Минимальный размер для тача */
  min-width: 44px;
  padding: 12px;
}

/* Устойчивые к тряске анимации */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* Темный режим */
@media (prefers-color-scheme: dark) {
  :root {
    --text-primary: #ffffff;
    --text-secondary: #cccccc;
    --background: #121212;
  }
}
</style>
```

### 2. Семантическая Структура

```html
<!-- Правильная семантическая структура -->
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Заголовок страницы - Название сайта</title>
  
  <!-- Предзагрузка критических ресурсов -->
  <link rel="preload" href="main.css" as="style">
  <link rel="preload" href="main.js" as="script">
  
  <!-- Основные стили -->
  <link rel="stylesheet" href="main.css">
  
  <!-- Мета-теги для доступности -->
  <meta name="description" content="Краткое описание страницы для поисковиков и скринридеров">
</head>
<body>
  <!-- Пропуск навигации для клавиатурных пользователей -->
  <a href="#main-content" class="skip-link">Перейти к основному содержимому</a>
  
  <header role="banner">
    <h1>Название сайта</h1>
    
    <nav role="navigation" aria-label="Основная навигация">
      <ul>
        <li><a href="/">Главная</a></li>
        <li><a href="/about">О нас</a></li>
        <li><a href="/services">Услуги</a></li>
        <li><a href="/contact">Контакты</a></li>
      </ul>
    </nav>
  </header>
  
  <main id="main-content" role="main">
    <article>
      <header>
        <h2>Заголовок статьи</h2>
        <p class="meta">
          <time datetime="2023-07-15">15 июля 2023</time> • 
          <span>Автор: Иван Петров</span>
        </p>
      </header>
      
      <section>
        <h3>Первый раздел</h3>
        <p>Содержание первого раздела...</p>
      </section>
      
      <section>
        <h3>Второй раздел</h3>
        <p>Содержание второго раздела...</p>
      </section>
    </article>
  </main>
  
  <aside role="complementary" aria-label="Дополнительная информация">
    <h3>Похожие статьи</h3>
    <ul>
      <li><a href="/related1">Связанная статья 1</a></li>
      <li><a href="/related2">Связанная статья 2</a></li>
    </ul>
  </aside>
  
  <footer role="contentinfo">
    <p>&copy; 2023 Название сайта. Все права защищены.</p>
    
    <nav aria-label="Дополнительная навигация">
      <ul>
        <li><a href="/privacy">Политика конфиденциальности</a></li>
        <li><a href="/terms">Условия использования</a></li>
      </ul>
    </nav>
  </footer>
  
  <!-- Скрипты в конце body -->
  <script src="main.js"></script>
</body>
</html>
```

### 3. Компоненты с Доступностью

```javascript
// Доступный компонент аккордеона
class AccessibleAccordion {
  constructor(element) {
    this.element = element;
    this.panels = [];
    this.buttons = [];
    this.init();
  }
  
  init() {
    // Находим все заголовки аккордеона
    const headers = this.element.querySelectorAll('[data-accordion-header]');
    
    headers.forEach((header, index) => {
      const button = header.querySelector('button');
      const panel = header.nextElementSibling;
      
      if (button && panel) {
        // Устанавливаем ARIA атрибуты
        button.setAttribute('aria-expanded', 'false');
        button.setAttribute('aria-controls', `panel-${index}`);
        
        panel.setAttribute('id', `panel-${index}`);
        panel.setAttribute('role', 'region');
        panel.setAttribute('aria-labelledby', button.id);
        panel.hidden = true;
        
        // Добавляем обработчики событий
        button.addEventListener('click', () => this.togglePanel(index));
        button.addEventListener('keydown', (event) => this.handleKeydown(event, index));
        
        this.buttons.push(button);
        this.panels.push(panel);
      }
    });
  }
  
  togglePanel(index) {
    const button = this.buttons[index];
    const panel = this.panels[index];
    
    const isExpanded = button.getAttribute('aria-expanded') === 'true';
    
    button.setAttribute('aria-expanded', !isExpanded);
    panel.hidden = isExpanded;
    
    // Отправляем событие для отслеживания
    this.element.dispatchEvent(new CustomEvent('accordion-toggle', {
      detail: { index, expanded: !isExpanded }
    }));
  }
  
  handleKeydown(event, index) {
    switch (event.key) {
      case 'Enter':
      case ' ':
        event.preventDefault();
        this.togglePanel(index);
        break;
        
      case 'ArrowDown':
        event.preventDefault();
        this.focusNext(index);
        break;
        
      case 'ArrowUp':
        event.preventDefault();
        this.focusPrevious(index);
        break;
        
      case 'Home':
        event.preventDefault();
        this.focusFirst();
        break;
        
      case 'End':
        event.preventDefault();
        this.focusLast();
        break;
    }
  }
  
  focusNext(currentIndex) {
    const nextIndex = (currentIndex + 1) % this.buttons.length;
    this.buttons[nextIndex].focus();
  }
  
  focusPrevious(currentIndex) {
    const prevIndex = (currentIndex - 1 + this.buttons.length) % this.buttons.length;
    this.buttons[prevIndex].focus();
  }
  
  focusFirst() {
    this.buttons[0].focus();
  }
  
  focusLast() {
    this.buttons[this.buttons.length - 1].focus();
  }
}

// Использование
document.addEventListener('DOMContentLoaded', () => {
  const accordions = document.querySelectorAll('[data-accordion]');
  accordions.forEach(accordion => new AccessibleAccordion(accordion));
});
```

## Связанные Концепции

- [[Архитектура]]
- [[Компонентная Архитектура]]
- [[Архитектура Интернационализации и Локализации]]
- [[Дизайн для Всех]]
- [[Пользовательский Опыт]]

## Теги

#accessibility #a11y #frontend #architecture #inclusive-design #wcag #screen-readers #keyboard-navigation #semantic-html