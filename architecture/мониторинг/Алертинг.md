---
aliases: ["Система алертинга", "Frontend Alerting", "Alert System"]
tags: 
  - monitoring
  - alerting
  - frontend
  - incident
---

# Алертинг фронтенд-приложений

## Обзор

Алертинг фронтенд-приложений - это система уведомлений, предназначенная для своевременного информирования команды разработчиков о критических проблемах, ошибках и аномалиях в работе клиентской части веб-приложений. Эффективная система алертинга позволяет быстро реагировать на инциденты и минимизировать время простоя сервиса.

## Типы алертов

### 1. Алерты ошибок (Error Alerts)

Срабатывают при возникновении критических ошибок:

- JavaScript ошибки
- Сетевые ошибки
- Ошибки API
- Ошибки загрузки ресурсов

```javascript
// Пример системы алертинга ошибок
class ErrorAlertSystem {
  constructor(thresholds = {}) {
    this.errorCount = 0;
    this.errorThreshold = thresholds.errorThreshold || 10; // за 5 минут
    this.timeWindow = thresholds.timeWindow || 5 * 60 * 1000; // 5 минут
    this.errors = [];
  }
  
  reportError(error) {
    const now = Date.now();
    this.errors = this.errors.filter(error => now - error.timestamp < this.timeWindow);
    
    this.errors.push({
      error: error,
      timestamp: now
    });
    
    if (this.errors.length >= this.errorThreshold) {
      this.triggerAlert({
        type: 'error_spike',
        message: `Обнаружено аномальное количество ошибок: ${this.errors.length}`,
        severity: 'high',
        data: {
          errorRate: this.errors.length / (this.timeWindow / 1000 / 60),
          recentErrors: this.errors.slice(-5).map(e => e.error.message)
        }
      });
    }
  }
  
  triggerAlert(alert) {
    // Отправка алерта в систему уведомлений
    this.sendAlertToNotificationService(alert);
    
    // Логирование алерта
    console.warn('ALERT TRIGGERED:', alert);
  }
}
```

### 2. Алерты производительности (Performance Alerts)

Срабатывают при ухудшении метрик производительности:

- Увеличение LCP (Largest Contentful Paint)
- Рост FID (First Input Delay)
- Увеличение CLS (Cumulative Layout Shift)
- Падение скорости загрузки страниц

```javascript
// Алертинг на основе Web Vitals
class PerformanceAlertSystem {
  constructor(thresholds = {}) {
    this.lcpThreshold = thresholds.lcp || 2500; // 2.5 секунды
    this.fidThreshold = thresholds.fid || 100;  // 100мс
    this.clsThreshold = thresholds.cls || 0.1;  // 0.1
  }
  
  checkPerformanceMetrics(metrics) {
    const alerts = [];
    
    if (metrics.lcp > this.lcpThreshold) {
      alerts.push({
        type: 'lcp_exceeded',
        severity: 'medium',
        message: `LCP превысил порог: ${metrics.lcp}ms > ${this.lcpThreshold}ms`,
        data: metrics
      });
    }
    
    if (metrics.fid > this.fidThreshold) {
      alerts.push({
        type: 'fid_exceeded',
        severity: 'high',
        message: `FID превысил порог: ${metrics.fid}ms > ${this.fidThreshold}ms`,
        data: metrics
      });
    }
    
    if (metrics.cls > this.clsThreshold) {
      alerts.push({
        type: 'cls_exceeded',
        severity: 'medium',
        message: `CLS превысил порог: ${metrics.cls} > ${this.clsThreshold}`,
        data: metrics
      });
    }
    
    alerts.forEach(alert => this.triggerAlert(alert));
  }
}
```

### 3. Алерты доступности (Availability Alerts)

Срабатывают при недоступности приложения или его компонентов:

- Ошибки загрузки скриптов
- Проблемы с подключением к API
- Ошибки при загрузке критических ресурсов

### 4. Алерты пользовательского опыта (UX Alerts)

Срабатывают при проблемах с пользовательским опытом:

- Долгие загрузки страниц
- Проблемы с интерактивностью
- Ошибки валидации форм

## Уровни важности алертов

### 1. Критические (Critical)

- Приложение полностью недоступно
- Критические ошибки, влияющие на всех пользователей
- Срочное вмешательство в течение 15-30 минут

### 2. Высокий (High)

- Значительное ухудшение производительности
- Ошибки, влияющие на большинство пользователей
- Реагирование в течение 1-2 часов

### 3. Средний (Medium)

- Умеренные проблемы с UX
- Ошибки, влияющие на небольшую часть пользователей
- Реагирование в течение 4-8 часов

### 4. Низкий (Low)

- Потенциальные проблемы
- Незначительные отклонения от нормы
- Плановое реагирование

## Реализация системы алертинга

### 1. Сбор данных для алертинга

```javascript
// Система сбора данных для алертинга
class AlertingDataCollector {
  constructor() {
    this.metrics = {
      errors: [],
      performance: [],
      network: [],
      userActions: []
    };
    
    this.setupCollectors();
  }
  
  setupCollectors() {
    // Сбор ошибок
    window.addEventListener('error', (event) => {
      this.metrics.errors.push({
        message: event.message,
        filename: event.filename,
        lineno: event.lineno,
        timestamp: Date.now()
      });
    });
    
    // Сбор производительности
    import('web-vitals').then(vitals => {
      vitals.getLCP(entry => this.metrics.performance.push(entry));
      vitals.getFID(entry => this.metrics.performance.push(entry));
      vitals.getCLS(entry => this.metrics.performance.push(entry));
    });
    
    // Сбор сетевых ошибок
    const originalFetch = window.fetch;
    window.fetch = function(...args) {
      return originalFetch.apply(this, args).then(response => {
        if (!response.ok) {
          this.metrics.network.push({
            url: args[0],
            status: response.status,
            timestamp: Date.now()
          });
        }
        return response;
      }).catch(error => {
        this.metrics.network.push({
          url: args[0],
          error: error.message,
          timestamp: Date.now()
        });
        throw error;
      });
    }.bind(this);
  }
  
  getMetrics() {
    return this.metrics;
  }
}
```

### 2. Обработка и анализ данных

```javascript
// Система анализа данных для алертинга
class AlertAnalyzer {
  constructor(alertSystem) {
    this.alertSystem = alertSystem;
    this.rules = [];
  }
  
  addRule(rule) {
    this.rules.push(rule);
  }
  
  analyze(metrics) {
    this.rules.forEach(rule => {
      const alerts = rule.evaluate(metrics);
      if (alerts.length > 0) {
        alerts.forEach(alert => this.alertSystem.triggerAlert(alert));
      }
    });
  }
}

// Пример правила алертинга
class HighErrorRateRule {
  constructor(windowMs = 300000, threshold = 50) { // 5 минут, 50 ошибок
    this.windowMs = windowMs;
    this.threshold = threshold;
  }
  
  evaluate(metrics) {
    const now = Date.now();
    const recentErrors = metrics.errors.filter(
      error => now - error.timestamp <= this.windowMs
    );
    
    if (recentErrors.length >= this.threshold) {
      return [{
        type: 'high_error_rate',
        severity: 'high',
        message: `Обнаружена высокая частота ошибок: ${recentErrors.length} за последние ${this.windowMs / 60000} минут`,
        data: {
          errorCount: recentErrors.length,
          timeWindow: this.windowMs,
          recentErrorTypes: [...new Set(recentErrors.map(e => e.message))]
        }
      }];
    }
    
    return [];
  }
}
```

### 3. Отправка уведомлений

```javascript
// Система отправки уведомлений
class NotificationService {
  constructor(config) {
    this.channels = config.channels || [];
    this.severityMap = config.severityMap || {
      'low': ['email'],
      'medium': ['email', 'slack'],
      'high': ['email', 'slack', 'sms'],
      'critical': ['email', 'slack', 'sms', 'phone']
    };
  }
  
  sendAlert(alert) {
    const channels = this.severityMap[alert.severity] || ['email'];
    
    channels.forEach(channel => {
      switch (channel) {
        case 'email':
          this.sendEmail(alert);
          break;
        case 'slack':
          this.sendSlackNotification(alert);
          break;
        case 'sms':
          this.sendSMS(alert);
          break;
        case 'phone':
          this.makePhoneCall(alert);
          break;
      }
    });
  }
  
  sendEmail(alert) {
    // Реализация отправки email уведомления
    fetch('/api/notifications/email', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        subject: `Алерт: ${alert.type}`,
        body: alert.message,
        recipients: ['dev-team@company.com']
      })
    }).catch(err => console.error('Email notification failed:', err));
  }
  
  sendSlackNotification(alert) {
    // Реализация отправки Slack уведомления
    fetch('/api/notifications/slack', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        text: `🚨 *${alert.severity.toUpperCase()}* - ${alert.message}`,
        channel: '#alerts',
        attachments: [{
          color: this.getColorForSeverity(alert.severity),
          fields: [
            { title: 'Тип', value: alert.type, short: true },
            { title: 'Важность', value: alert.severity, short: true }
          ]
        }]
      })
    }).catch(err => console.error('Slack notification failed:', err));
  }
  
  getColorForSeverity(severity) {
    const colors = {
      'critical': '#ff0000',
      'high': '#ff4444',
      'medium': '#ffaa00',
      'low': '#ffff00'
    };
    return colors[severity] || '#808080';
  }
}
```

## Интеграция с существующими системами

### 1. Интеграция с PagerDuty

```javascript
// Интеграция с PagerDuty для критических алертов
class PagerDutyIntegration {
  constructor(apiKey, serviceId) {
    this.apiKey = apiKey;
    this.serviceId = serviceId;
  }
  
  triggerIncident(alert) {
    fetch('https://events.pagerduty.com/v2/enqueue', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        routing_key: this.apiKey,
        event_action: 'trigger',
        payload: {
          summary: alert.message,
          source: window.location.href,
          severity: this.mapSeverity(alert.severity),
          custom_details: alert.data
        }
      })
    }).catch(err => console.error('PagerDuty integration failed:', err));
  }
  
  mapSeverity(severity) {
    const mapping = {
      'critical': 'critical',
      'high': 'error',
      'medium': 'warning',
      'low': 'info'
    };
    return mapping[severity] || 'info';
  }
}
```

### 2. Интеграция с Sentry

```javascript
// Интеграция с Sentry для трассировки ошибок
class SentryIntegration {
  constructor(dsn) {
    this.dsn = dsn;
    this.setupSentry();
  }
  
  setupSentry() {
    // Упрощенная интеграция с Sentry
    if (typeof Sentry !== 'undefined') {
      Sentry.init({ dsn: this.dsn });
    }
  }
  
  captureException(error, context) {
    if (typeof Sentry !== 'undefined') {
      Sentry.withScope(scope => {
        Object.keys(context).forEach(key => {
          scope.setContext(key, context[key]);
        });
        Sentry.captureException(error);
      });
    }
  }
}
```

## Тестирование системы алертинга

```javascript
// Система тестирования алертинга
class AlertingTester {
  constructor(alertSystem) {
    this.alertSystem = alertSystem;
    this.testResults = [];
  }
  
  runTests() {
    // Тест на срабатывание алерта при высокой частоте ошибок
    this.testHighErrorRate();
    
    // Тест на срабатывание алерта при ухудшении производительности
    this.testPerformanceDegradation();
    
    return this.testResults;
  }
  
  testHighErrorRate() {
    const errorAlert = {
      type: 'high_error_rate',
      severity: 'high',
      message: 'Тест высокой частоты ошибок',
      data: { errorCount: 100, timeWindow: 300000 }
    };
    
    try {
      this.alertSystem.triggerAlert(errorAlert);
      this.testResults.push({
        test: 'high_error_rate',
        status: 'passed',
        message: 'Алерт успешно сработал'
      });
    } catch (error) {
      this.testResults.push({
        test: 'high_error_rate',
        status: 'failed',
        message: error.message
      });
    }
  }
}
```

## Заключение

Эффективная система алертинга фронтенд-приложений должна быть сбалансированной - не слишком чувствительной (чтобы избежать "шума") и не слишком инертной (чтобы не пропустить реальные проблемы). Она должна обеспечивать своевременное уведомление команды о критических инцидентах и помогать поддерживать высокое качество пользовательского опыта.

## См. также

- [[Архитектура-мониторинга]]
- [[Сбор-метрик]]
- [[Логирование]]
- [[Аналитика]]