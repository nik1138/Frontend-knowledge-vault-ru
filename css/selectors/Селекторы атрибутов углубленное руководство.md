---
aliases: ["Селекторы атрибутов", "CSS атрибуты", "Продвинутые селекторы"]
tags: [css, selectors, attributes, advanced, matching]
---

# Селекторы атрибутов: углубленное руководство

## Введение

Селекторы атрибутов CSS позволяют выбирать элементы на основе наличия или значений их HTML-атрибутов. Это мощный инструмент для создания гибких и точных селекторов, особенно полезный при работе с динамическим контентом и компонентами.

## Базовые селекторы атрибутов

### Простое наличие атрибута

```css
/* Выбирает все элементы, у которых есть атрибут title */
[title] {
  border-bottom: 1px dotted #007bff;
}

/* Выбирает все изображения с атрибутом alt */
img[alt] {
  margin: 5px;
}
```

### Точное совпадение значения

```css
/* Выбирает элементы с точным совпадением значения атрибута */
input[type="text"] {
  padding: 8px;
  border: 1px solid #ccc;
}

/* Выбирает ссылки с определенным классом */
a[class="external"] {
  color: #dc3545;
}
```

## Продвинутые операторы сопоставления

### Оператор ^= (начинается с)

```css
/* Выбирает элементы, у которых значение атрибута начинается с определенной строки */
[class^="btn-"] {
  padding: 10px 15px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

/* Пример: .btn-primary, .btn-secondary, .btn-success */
a[href^="https://"] {
  position: relative;
  padding-right: 15px;
}

a[href^="https://"]::after {
  content: "🔒";
  position: absolute;
  right: 0;
}
```

### Оператор $= (заканчивается на)

```css
/* Выбирает элементы, у которых значение атрибута заканчивается определенной строкой */
img[src$=".png"] {
  border: 2px solid #28a745;
}

a[href$=".pdf"] {
  position: relative;
  padding-right: 20px;
}

a[href$=".pdf"]::after {
  content: "📄";
  position: absolute;
  right: 0;
}

/* Выбирает email поля */
input[name$="email"] {
  background-color: #f8f9fa;
}
```

### Оператор *= (содержит)

```css
/* Выбирает элементы, у которых значение атрибута содержит определенную подстроку */
[class*="nav"] {
  display: flex;
  list-style: none;
}

/* Выбирает ссылки, содержащие определенный домен */
a[href*="example.com"] {
  color: #6f42c1;
}

/* Выбирает элементы с определенными данными */
div[data-category*="tech"] {
  background-color: #e7f3ff;
}
```

### Оператор |= (начинается с и отделен дефисом)

```css
/* Полезен для языковых атрибутов */
[lang|="en"] {
  quotes: "‘" "’" "“" "”";
}

/* Пример: выбирает lang="en", lang="en-US", lang="en-GB" */
[lang|="ru"] {
  quotes: "«" "»";
}
```

### Оператор ~= (содержит слово)

```css
/* Выбирает элементы, у которых атрибут class содержит определенное слово */
[title~="important"] {
  font-weight: bold;
  color: #dc3545;
}

/* Эквивалентно .important, но через атрибут title */
```

## Современные возможности селекторов атрибутов

### Чувствительность к регистру

```css
/* По умолчанию регистрозависимо для значений атрибутов */
[data-value="HIGH"] {
  color: #dc3545;
}

/* Использование модификатора i для регистронезависимого поиска */
input[type="TEXT" i] {
  background-color: #f8f9fa;
}

/* Регистронезависимый поиск в значениях */
[data-category="TECH" i] {
  background-color: #e7f3ff;
}
```

### Комбинация с другими селекторами

```css
/* Комбинация с ID селектором */
#main-content [data-type="article"] {
  margin-bottom: 20px;
}

/* Комбинация с псевдоклассами */
input[type="text"]:focus {
  outline: 2px solid #007bff;
}

/* Комбинация с :not() */
input:not([type="hidden"]):not([disabled]) {
  padding: 8px;
  border: 1px solid #ccc;
  border-radius: 4px;
}
```

## Практические примеры использования

### Формы и валидация

```css
/* Стилизация обязательных полей */
input[required] {
  border-left: 3px solid #007bff;
}

/* Стилизация полей с ошибками */
input[aria-invalid="true"] {
  border-color: #dc3545;
  box-shadow: 0 0 0 2px rgba(220, 53, 69, 0.25);
}

/* Стилизация полей с успехом */
input[aria-invalid="false"] {
  border-color: #28a745;
}

/* Стилизация полей на основе типа */
input[type="email"] {
  text-transform: lowercase;
}

input[type="password"] {
  font-family: monospace;
}
```

### Навигация и меню

```css
/* Стилизация активных ссылок */
a[aria-current="page"] {
  font-weight: bold;
  color: #007bff;
  border-bottom: 2px solid #007bff;
}

/* Стилизация внешних ссылок */
a[href^="http"]:not([href*="mysite.com"]) {
  position: relative;
  padding-right: 15px;
}

a[href^="http"]:not([href*="mysite.com"])::after {
  content: "↗";
  position: absolute;
  right: 0;
  font-size: 0.8em;
}
```

### Компоненты и виджеты

```css
/* Стилизация кнопок на основе типа */
button[data-type="primary"] {
  background-color: #007bff;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
}

button[data-type="secondary"] {
  background-color: transparent;
  color: #007bff;
  border: 1px solid #007bff;
  padding: 10px 20px;
  border-radius: 4px;
}

/* Стилизация карточек на основе состояния */
.card[data-status="featured"] {
  border: 2px solid gold;
  box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}

.card[data-status="sold"] {
  opacity: 0.6;
  text-decoration: line-through;
}

/* Стилизация элементов на основе данных */
.item[data-priority="high"] {
  border-left: 4px solid #dc3545;
}

.item[data-priority="low"] {
  border-left: 4px solid #6c757d;
}
```

## Сложные комбинации селекторов атрибутов

### Множественные условия

```css
/* Элементы с несколькими атрибутами */
input[type="text"][required][aria-invalid="false"] {
  border-color: #28a745;
  background-color: #f8fff8;
}

/* Сложные условия с разными операторами */
a[href^="https://"][href*="secure"][href$=".com"] {
  background-color: #e8f5e8;
  padding: 5px;
  border-radius: 3px;
}
```

### Комбинация с :is() и :where()

```css
/* Группировка селекторов атрибутов */
:is([data-type="primary"], [data-type="secondary"], [data-type="danger"]) {
  padding: 10px 15px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

/* Сброс стилей с :where() */
:where([type="submit"], [type="button"], [type="reset"]) {
  padding: 10px 20px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}
```

### Использование с :has()

```css
/* Современный подход с :has() */
/* Стилизация родительского элемента на основе дочернего атрибута */
.form-group:has(input[aria-invalid="true"]) {
  border: 2px solid #dc3545;
  border-radius: 8px;
  padding: 10px;
}

/* Стилизация контейнера с определенными элементами */
.card:has([data-urgent="true"]) {
  border: 2px solid #dc3545;
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0% { box-shadow: 0 0 0 0 rgba(220, 53, 69, 0.4); }
  70% { box-shadow: 0 0 0 10px rgba(220, 53, 69, 0); }
  100% { box-shadow: 0 0 0 0 rgba(220, 53, 69, 0); }
}
```

## Производительность селекторов атрибутов

### Наиболее производительные паттерны

```css
/* Наиболее производительные */
[type="text"] { }          /* Быстрее всего */
input[type="text"] { }     /* Немного медленнее */
div[type="text"] { }       /* Медленнее */

/* Менее производительные */
[class*="button"] { }      /* Медленный - поиск подстроки */
[title~="warning"] { }     /* Медленный - анализ слов */
[href^="https://"] { }     /* Относительно медленный */
```

### Оптимизация сложных селекторов

```css
/* ПЛОХО: медленный селектор */
div[class*="container"] > section[data-type*="content"] p[title*="important"] {
  color: red;
}

/* ЛУЧШЕ: использование классов */
.important-content {
  color: red;
}

/* АЛЬТЕРНАТИВА: оптимизированный атрибутный селектор */
[data-type="important-content"] {
  color: red;
}
```

## Практические примеры из реальных проектов

### Управление состоянием компонентов

```css
/* Состояния переключателя */
.toggle[data-state="on"] {
  background-color: #28a745;
  color: white;
}

.toggle[data-state="off"] {
  background-color: #6c757d;
  color: white;
}

/* Состояния прогресса */
.progress-bar[data-completion="complete"] {
  background: linear-gradient(to right, #28a745, #28a745);
}

.progress-bar[data-completion="in-progress"] {
  background: linear-gradient(to right, #28a745, #e9ecef);
}

.progress-bar[data-completion="not-started"] {
  background: #e9ecef;
}
```

### Темизация интерфейса

```css
/* Селекторы для тем */
.card[data-theme="dark"] {
  background-color: #343a40;
  color: #fff;
}

.card[data-theme="light"] {
  background-color: #fff;
  color: #212529;
}

/* Стилизация на основе размера */
.button[data-size="small"] { padding: 5px 10px; font-size: 0.8em; }
.button[data-size="medium"] { padding: 8px 15px; font-size: 1em; }
.button[data-size="large"] { padding: 12px 25px; font-size: 1.2em; }
```

### Адаптивные компоненты

```css
/* Стилизация на основе ориентации */
.container[data-orientation="horizontal"] {
  display: flex;
  flex-direction: row;
}

.container[data-orientation="vertical"] {
  display: flex;
  flex-direction: column;
}

/* Стилизация на основе количества элементов */
.grid[data-count="1"] { grid-template-columns: 1fr; }
.grid[data-count="2"] { grid-template-columns: repeat(2, 1fr); }
.grid[data-count="3"] { grid-template-columns: repeat(3, 1fr); }
.grid[data-count="4"] { grid-template-columns: repeat(4, 1fr); }
```

## Лучшие практики

### Организация атрибутов

```css
/* Используйте семантически значимые имена атрибутов */
/* ПРЕДПОЧТИТЕЛЬНО */
[data-status="active"]
[data-type="primary"]
[data-priority="high"]

/* ИЗБЕГАЙТЕ */
[data-x="1"]
[data-val="a"]
[data-t="p"]
```

### Совместимость и фолбеки

```css
/* Обеспечьте фолбеки для старых браузеров */
.button {
  /* Базовые стили */
  padding: 10px 20px;
  border: 1px solid #ccc;
}

.button[data-type="primary"] {
  /* Специфические стили */
  background-color: #007bff;
  color: white;
  border-color: #007bff;
}
```

## Заключение

Селекторы атрибутов предоставляют мощный способ стилизации элементов на основе их HTML-атрибутов. Они особенно полезны для создания гибких компонентов и работы с динамическим контентом. При правильном использовании они могут значительно упростить структуру CSS и улучшить поддерживаемость кода.

## Связанные темы

- [[Продвинутые комбинаторы и их производительность]]
- [[Оптимизация селекторов для лучшей производительности]]
- [[CSS Selectors]]
- [[CSS Data Attributes]]