---
aliases: ["Смена тем", "Переключатель тем", "Dynamic Themes"]
tags: [css, темизация, javascript, frontend]
---

# Переключение тем

Переключение тем - это процесс динамического изменения визуального оформления веб-страницы на основе пользовательских предпочтений или системных настроек. В 2025 году это стало стандартом для современных веб-приложений, позволяя пользователям выбирать наиболее удобный режим отображения контента.

## Основы переключения тем

Переключение тем обычно реализуется с использованием CSS-переменных и JavaScript. Процесс включает:

- [[CSS переменные]] для хранения значений темы
- [[LocalStorage]] для сохранения выбора пользователя
- [[Event listeners]] для отслеживания изменений
- [[Темные темы]] для автоматического переключения

## Простая реализация переключения тем

### HTML-структура

```html
<div class="theme-toggle">
  <button id="theme-toggle-btn" aria-label="Переключить тему">
    <span class="sun-icon">☀️</span>
    <span class="moon-icon">🌙</span>
  </button>
</div>
```

### CSS-переменные для тем

```css
:root {
  --bg-color: #ffffff;
  --text-color: #333333;
  --surface-color: #f8f9fa;
  --border-color: #dee2e6;
}

[data-theme="dark"] {
  --bg-color: #1a1a1a;
  --text-color: #f0f0f0;
  --surface-color: #2d2d2d;
  --border-color: #444444;
}
```

### JavaScript для переключения тем

```javascript
class ThemeSwitcher {
  constructor() {
    this.themeToggleBtn = document.getElementById('theme-toggle-btn');
    this.currentTheme = this.getStoredTheme() || this.getSystemTheme();
    this.init();
  }

  init() {
    this.setTheme(this.currentTheme);
    this.bindEvents();
  }

  getStoredTheme() {
    return localStorage.getItem('theme');
  }

  getSystemTheme() {
    return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
  }

  setTheme(theme) {
    document.documentElement.setAttribute('data-theme', theme);
    localStorage.setItem('theme', theme);
    this.updateIcon(theme);
  }

  toggleTheme() {
    const newTheme = this.currentTheme === 'light' ? 'dark' : 'light';
    this.setTheme(newTheme);
    this.currentTheme = newTheme;
  }

  updateIcon(theme) {
    const sunIcon = this.themeToggleBtn.querySelector('.sun-icon');
    const moonIcon = this.themeToggleBtn.querySelector('.moon-icon');
    
    if (theme === 'dark') {
      sunIcon.style.display = 'inline';
      moonIcon.style.display = 'none';
    } else {
      sunIcon.style.display = 'none';
      moonIcon.style.display = 'inline';
    }
  }

  bindEvents() {
    this.themeToggleBtn.addEventListener('click', () => this.toggleTheme());
    
    // Следим за системными настройками
    window.matchMedia('(prefers-color-scheme: dark)')
      .addEventListener('change', (e) => {
        if (!this.getStoredTheme()) {
          this.setTheme(e.matches ? 'dark' : 'light');
          this.currentTheme = e.matches ? 'dark' : 'light';
        }
      });
  }
}

// Инициализация
document.addEventListener('DOMContentLoaded', () => {
  new ThemeSwitcher();
});
```

## Переключение с анимацией

Для более плавного переключения тем можно добавить CSS-анимацию:

```css
:root {
  --transition-duration: 0.3s;
}

html {
  transition: background-color var(--transition-duration) ease,
              color var(--transition-duration) ease;
}

/* Для отдельных элементов */
.button, .card {
  transition: background-color var(--transition-duration) ease,
              border-color var(--transition-duration) ease,
              color var(--transition-duration) ease;
}
```

## Продвинутая реализация с несколькими темами

Возможна реализация с поддержкой нескольких тем:

```javascript
class AdvancedThemeSwitcher {
  constructor() {
    this.themes = {
      light: 'light',
      dark: 'dark',
      blue: 'blue',
      sepia: 'sepia'
    };
    
    this.defaultTheme = 'light';
    this.currentTheme = this.getStoredTheme() || this.defaultTheme;
    this.init();
  }

  init() {
    this.createThemeSelector();
    this.setTheme(this.currentTheme);
    this.bindEvents();
  }

  createThemeSelector() {
    const selector = document.createElement('select');
    selector.id = 'theme-selector';
    
    Object.entries(this.themes).forEach(([key, value]) => {
      const option = document.createElement('option');
      option.value = value;
      option.textContent = this.getThemeDisplayName(value);
      selector.appendChild(option);
    });
    
    selector.value = this.currentTheme;
    document.body.appendChild(selector);
  }

  getThemeDisplayName(theme) {
    const names = {
      light: 'Светлая',
      dark: 'Темная',
      blue: 'Синяя',
      sepia: 'Сепия'
    };
    return names[theme] || theme;
  }

  setTheme(theme) {
    // Удаляем все классы тем
    Object.values(this.themes).forEach(t => {
      document.documentElement.classList.remove(`theme-${t}`);
    });
    
    // Добавляем новый класс темы
    document.documentElement.classList.add(`theme-${theme}`);
    
    // Устанавливаем атрибут
    document.documentElement.setAttribute('data-theme', theme);
    
    // Сохраняем в localStorage
    localStorage.setItem('theme', theme);
    
    this.currentTheme = theme;
  }

  getStoredTheme() {
    return localStorage.getItem('theme');
  }

  bindEvents() {
    const selector = document.getElementById('theme-selector');
    if (selector) {
      selector.addEventListener('change', (e) => {
        this.setTheme(e.target.value);
      });
    }
  }
}
```

## Сохранение темы между сессиями

Для сохранения темы между сессиями используется localStorage:

```javascript
// Сохранение темы
localStorage.setItem('theme', 'dark');

// Получение темы
const savedTheme = localStorage.getItem('theme');

// Удаление темы (если нужно сбросить)
localStorage.removeItem('theme');
```

## Интеграция с системными настройками

Можно интегрировать переключение тем с системными настройками:

```javascript
// Проверяем системную тему
const prefersDarkScheme = window.matchMedia('(prefers-color-scheme: dark)');

// Устанавливаем тему в зависимости от системной
const systemTheme = prefersDarkScheme.matches ? 'dark' : 'light';

// Слушаем изменения системной темы
prefersDarkScheme.addEventListener('change', (e) => {
  if (!localStorage.getItem('theme')) {
    // Только если пользователь не выбрал тему явно
    document.documentElement.setAttribute('data-theme', e.matches ? 'dark' : 'light');
  }
});
```

## Рекомендации по переключению тем

1. **Сохраняйте выбор пользователя** - используйте localStorage для запоминания предпочтений
2. **Учитывайте системные настройки** - автоматически применяйте тему в соответствии с настройками ОС
3. **Добавляйте анимации** - плавные переходы улучшают пользовательский опыт
4. **Тестируйте доступность** - обеспечьте достаточный контраст в обеих темах
5. **Оптимизируйте производительность** - избегайте частых изменений стилей

## Заключение

Переключение тем - важная функция современных веб-приложений, позволяющая пользователям настраивать интерфейс под свои предпочтения. Правильная реализация обеспечивает комфортное взаимодействие с приложением в любое время суток и в любых условиях освещения.

См. также: [[Создание тем]], [[Темные темы]], [[Кастомизация тем]], [[Темизация компонентов]]