---
aliases: ["Bubble Sort", "Пузырьковая сортировка", "Сортировка методом пузырька"]
tags: [алгоритмы, сортировка, обучение, подготовка-к-собеседованиям]
---

# Сортировка пузырьком

## Определение

Сортировка пузырьком (Bubble Sort) - это простой алгоритм сортировки, который работает, многократно проходя по списку, сравнивая соседние элементы и меняя их местами, если они находятся в неправильном порядке. Процесс повторяется до тех пор, пока весь список не будет отсортирован.

## Принцип работы

Алгоритм последовательно просматривает массив, сравнивая пары соседних элементов. Если левый элемент больше правого (в сортировке по возрастанию), они меняются местами. Каждый проход "всплывает" наибольший элемент к концу массива, как пузырек - отсюда и название.

## Псевдокод

```
АЛГОРИТМ СортировкаПузырьком(массив)
    ВХОД: массив - неотсортированный массив элементов
    ВЫХОД: отсортированный массив элементов

    n = ДЛИНА(массив)
    FOR i = 0 ДО n-1:
        swapped = ЛОЖЬ
        FOR j = 0 ДО n-i-2:
            IF массив[j] > массив[j+1]:
                ОБМЕН(массив[j], массив[j+1])
                swapped = ИСТИНА
        END IF
        IF swapped = ЛОЖЬ:
            BREAK  // Массив уже отсортирован
        END IF
    END FOR
    ВОЗВРАТИТЬ массив
КОНЕЦ АЛГОРИТМА
```

## Реализация на Python

```python
def bubble_sort(arr):
    """
    Сортировка пузырьком
    :param arr: список элементов для сортировки
    :return: отсортированный список
    """
    n = len(arr)
    # Проходим по всем элементам массива
    for i in range(n):
        # Флаг для оптимизации - если за проход не было обменов, массив отсортирован
        swapped = False
        # Последние i элементов уже на своих местах
        for j in range(0, n - i - 1):
            # Меняем местами, если элемент найден больше следующего
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True
        # Если не было обменов, массив отсортирован
        if not swapped:
            break
    return arr

# Пример использования
numbers = [64, 34, 25, 12, 22, 11, 90]
print(f"До сортировки: {numbers}")
bubble_sort(numbers)
print(f"После сортировки: {numbers}")
```

## Реализация на JavaScript

```javascript
function bubbleSort(arr) {
    const n = arr.length;
    for (let i = 0; i < n; i++) {
        let swapped = false;
        for (let j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                // Меняем местами элементы
                [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];
                swapped = true;
            }
        }
        // Если не было обменов, массив отсортирован
        if (!swapped) {
            break;
        }
    }
    return arr;
}

// Пример использования
const numbers = [64, 34, 25, 12, 22, 11, 90];
console.log("До сортировки:", numbers);
bubbleSort([...numbers]); // Используем копию массива, чтобы не изменять исходный
console.log("После сортировки:", bubbleSort([...numbers]));
```

## Временная сложность

- **Лучший случай**: O(n) - когда массив уже отсортирован (с оптимизацией)
- **Средний случай**: O(n²)
- **Худший случай**: O(n²) - когда массив отсортирован в обратном порядке

## Пространственная сложность

O(1) - алгоритм сортировки на месте, не требует дополнительной памяти, кроме нескольких переменных.

## Преимущества

- Прост в понимании и реализации
- Не требует дополнительной памяти
- Устойчив (не меняет относительный порядок равных элементов)
- Хорошо работает с почти отсортированными массивами (с оптимизацией)

## Недостатки

- Низкая эффективность на больших массивах из-за квадратичной сложности
- Много обменов элементов, даже если массив почти отсортирован

## Практическое применение

Сортировка пузырьком в основном используется:

- В образовательных целях для понимания основ алгоритмов сортировки
- В системах, где данные почти отсортированы и размер массива мал (n < 50)
- В embedded системах с ограниченными ресурсами
- При обучении новых сотрудников как пример неэффективного алгоритма

> [!tip] Практический совет
> В промышленной разработке редко используется из-за низкой эффективности, но полезно знать для понимания принципов работы алгоритмов сортировки и прохождения собеседований.

> [!warning] Важно
> Не рекомендуется использовать в производстве для больших наборов данных. Вместо этого используйте встроенные функции сортировки языков программирования (например, `sort()` в JavaScript или `sorted()` в Python), которые реализуют более эффективные алгоритмы.

## Сравнение с другими алгоритмами

- [[Быстрая-сортировка]] - более эффективна с O(n log n) в среднем случае
- [[Сортировка-слиянием]] - стабильна с гарантированной O(n log n)
- [[Пирамидальная-сортировка]] - эффективна с O(n log n) и O(1) по памяти

## Заключение

Сортировка пузырьком - это классический пример простого, но неэффективного алгоритма сортировки. Знание этого алгоритма важно для понимания основ и прохождения технических собеседований, но не рекомендуется для использования в производстве.