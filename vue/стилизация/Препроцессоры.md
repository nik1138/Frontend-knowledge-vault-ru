---
aliases: ["CSS Preprocessors", "Препроцессоры CSS", "Sass, Less, Stylus"]
tags: [vue, css, препроцессоры, sass, less, stylus, стилизация]
---

# Препроцессоры CSS в Vue.js

## Введение

Препроцессоры CSS - это инструменты, которые расширяют возможности стандартного CSS, добавляя такие функции, как переменные, вложенные правила, миксины, наследование классов и другие программные конструкции. В экосистеме Vue.js наиболее популярными являются Sass/SCSS, Less и Stylus.

## Основные препроцессоры

### Sass/SCSS

Sass (Syntactically Awesome Style Sheets) - один из самых популярных CSS-препроцессоров. Поддерживает два синтаксиса:
- SCSS (Sassy CSS) - синтаксис, похожий на обычный CSS
- Indented Syntax (Sass) - отступо-ориентированный синтаксис

#### Установка

```bash
npm install -D sass
```

#### Пример использования в Vue

```vue
<template>
  <div class="container">
    <h1 class="title">Заголовок компонента</h1>
    <p class="description">Описание компонента</p>
  </div>
</template>

<script>
export default {
  name: 'SassComponent'
}
</script>

<style lang="scss" scoped>
// Переменные
$primary-color: #42b983;
$secondary-color: #646cff;
$border-radius: 8px;
$padding: 20px;

// Вложенные правила
.container {
  padding: $padding;
  background-color: #f5f5f5;
  border-radius: $border-radius;
  
  .title {
    color: $primary-color;
    font-size: 24px;
    margin-bottom: 10px;
    
    &:hover {
      color: darken($primary-color, 10%);
    }
  }
  
  .description {
    color: #666;
    line-height: 1.6;
  }
}

// Миксины
@mixin button-style($bg-color) {
  background-color: $bg-color;
  border: none;
  border-radius: $border-radius;
  padding: 10px 20px;
  cursor: pointer;
  
  &:hover {
    background-color: darken($bg-color, 10%);
  }
}

.button-primary {
  @include button-style($primary-color);
}

.button-secondary {
  @include button-style($secondary-color);
}

// Функции
@function calculate-opacity($color, $opacity) {
  @return rgba($color, $opacity);
}

.semi-transparent {
  background-color: calculate-opacity(#000, 0.1);
}
</style>
```

#### Продвинутые возможности Sass

```scss
// Математические операции
$base-font-size: 16px;
$multiplier: 1.25;

.title {
  font-size: $base-font-size * $multiplier; // 20px
}

// Циклы
@for $i from 1 through 5 {
  .col-#{$i} {
    width: percentage($i / 12);
  }
}

// Условия
@mixin responsive-text($size) {
  @if $size == small {
    font-size: 12px;
  } @else if $size == medium {
    font-size: 16px;
  } @else {
    font-size: 20px;
  }
}

.text-small {
  @include responsive-text(small);
}

// Списки и maps
$colors: (
  primary: #42b983,
  secondary: #646cff,
  danger: #e74c3c
);

@each $name, $color in $colors {
  .text-#{$name} {
    color: $color;
  }
}
```

### Less

Less (Leaner Style Sheets) - CSS-препроцессор, синтаксически похожий на CSS, но с дополнительными возможностями.

#### Установка

```bash
npm install -D less
```

#### Пример использования в Vue

```vue
<template>
  <div class="container">
    <h1 class="title">Заголовок компонента</h1>
    <div class="content">
      <p class="text">Текст параграфа</p>
    </div>
  </div>
</template>

<style lang="less" scoped>
// Переменные
@primary-color: #42b983;
@secondary-color: #646cff;
@border-radius: 8px;
@padding: 20px;

// Вложенные правила
.container {
  padding: @padding;
  background-color: #f5f5f5;
  border-radius: @border-radius;
  
  .title {
    color: @primary-color;
    font-size: 24px;
    margin-bottom: 10px;
    
    &:hover {
      color: darken(@primary-color, 10%);
    }
  }
  
  .content {
    .text {
      color: #666;
      line-height: 1.6;
    }
  }
}

// Миксины
.button-style(@bg-color) {
  background-color: @bg-color;
  border: none;
  border-radius: @border-radius;
  padding: 10px 20px;
  cursor: pointer;
  
  &:hover {
    background-color: darken(@bg-color, 10%);
  }
}

.button-primary {
  .button-style(@primary-color);
}

.button-secondary {
  .button-style(@secondary-color);
}

// Параметризованные миксины
.transition(@duration: 0.3s, @property: all) {
  transition: @property @duration ease;
}

.fade-element {
  .transition(0.5s, opacity);
}
</style>
```

### Stylus

Stylus - гибкий CSS-препроцессор с возможностью использования различных синтаксисов.

#### Установка

```bash
npm install -D stylus
```

#### Пример использования в Vue

```vue
<template>
  <div class="container">
    <h1 class="title">Заголовок компонента</h1>
    <p class="description">Описание компонента</p>
  </div>
</template>

<style lang="stylus" scoped>
// Переменные
primary-color = #42b983
secondary-color = #646cff
border-radius = 8px
padding = 20px

// Вложенные правила
.container
  padding padding
  background-color #f5f5f5
  border-radius border-radius
  
  .title
    color primary-color
    font-size 24px
    margin-bottom 10px
    
    &:hover
      color primary-color - 10%

  .description
    color #666
    line-height 1.6

// Миксины
button-style(bg-color)
  background-color bg-color
  border none
  border-radius border-radius
  padding 10px 20px
  cursor pointer
  
  &:hover
    background-color bg-color - 10%

.button-primary
  button-style(primary-color)

.button-secondary
  button-style(secondary-color)

// Условия
responsive-text(size)
  if size == 'small'
    font-size 12px
  else if size == 'medium'
    font-size 16px
  else
    font-size 20px

.text-small
  responsive-text('small')
</style>
```

## Совместное использование с Vue

### Глобальные переменные и миксины

Создание общего файла с переменными:

```scss
// src/assets/scss/variables.scss
// Цвета
$primary-color: #42b983;
$secondary-color: #646cff;
$success-color: #2ecc71;
$warning-color: #f39c12;
$danger-color: #e74c3c;

// Размеры
$border-radius: 4px;
$border-radius-lg: 8px;
$spacing-xs: 0.25rem;
$spacing-sm: 0.5rem;
$spacing-md: 1rem;
$spacing-lg: 1.5rem;
$spacing-xl: 3rem;

// Типографика
$font-family-base: 'Roboto', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
$font-size-xs: 0.75rem;
$font-size-sm: 0.875rem;
$font-size-base: 1rem;
$font-size-lg: 1.125rem;
$font-size-xl: 1.25rem;
$font-size-2xl: 1.5rem;
$font-size-3xl: 1.875rem;
$font-size-4xl: 2.25rem;
$font-weight-normal: 400;
$font-weight-medium: 500;
$font-weight-semibold: 600;
$font-weight-bold: 700;

// Тени
$shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
$shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
$shadow-lg: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
```

Импорт глобальных переменных в компоненты:

```vue
<template>
  <div :class="$style.card">
    <h3 :class="$style.title">Заголовок карточки</h3>
    <p :class="$style.content">Содержимое карточки</p>
  </div>
</template>

<style lang="scss" module>
// Импорт глобальных переменных
@import '@/assets/scss/variables.scss';

.card {
  padding: $spacing-md;
  background-color: white;
  border: 1px solid #e0e0e0;
  border-radius: $border-radius-lg;
  box-shadow: $shadow;
  transition: box-shadow $transition-base;
  
  &:hover {
    box-shadow: $shadow-lg;
  }
}

.title {
  color: $primary-color;
  font-size: $font-size-lg;
  font-weight: $font-weight-semibold;
  margin-bottom: $spacing-sm;
}

.content {
  color: #666;
  line-height: 1.6;
}
</style>
```

### Настройка сборки для препроцессоров

#### В Vite

```javascript
// vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  css: {
    preprocessorOptions: {
      scss: {
        additionalData: `@import "@/assets/scss/variables.scss";`
      }
    }
  }
})
```

#### В Vue CLI

```javascript
// vue.config.js
module.exports = {
  css: {
    loaderOptions: {
      scss: {
        additionalData: `@import "@/assets/scss/variables.scss";`
      }
    }
  }
}
```

## Практические рекомендации

### 1. Организация файлов стилей

```
src/
├── assets/
│   └── scss/
│       ├── abstracts/
│       │   ├── _variables.scss
│       │   ├── _mixins.scss
│       │   └── _functions.scss
│       ├── base/
│       │   ├── _reset.scss
│       │   ├── _typography.scss
│       │   └── _utilities.scss
│       ├── components/
│       │   ├── _buttons.scss
│       │   ├── _cards.scss
│       │   └── _forms.scss
│       ├── layout/
│       │   ├── _grid.scss
│       │   └── _navigation.scss
│       └── main.scss
```

### 2. Создание миксинов для часто используемых паттернов

```scss
// src/assets/scss/abstracts/_mixins.scss

// Адаптивные шрифты
@mixin responsive-font($min-size: 14px, $max-size: 18px, $min-screen: 320px, $max-screen: 1200px) {
  font-size: $min-size;
  
  @media (min-width: $min-screen) {
    font-size: calc(#{$min-size} + #{($max-size - $min-size)} * ((100vw - #{$min-screen}) / #{$max-screen - $min-screen}));
  }
  
  @media (min-width: $max-screen) {
    font-size: $max-size;
  }
}

// Адаптивный контейнер
@mixin container($max-width: 1200px) {
  max-width: $max-width;
  margin: 0 auto;
  padding: 0 1rem;
  
  @media (min-width: 768px) {
    padding: 0 1.5rem;
  }
}

// Скрытие контента для скринридеров
@mixin sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

// Центрирование элемента
@mixin center-element {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}
```

### 3. Создание тем с помощью переменных

```scss
// src/assets/scss/abstracts/_themes.scss

// Светлая тема (по умолчанию)
:root {
  --color-primary: #42b983;
  --color-primary-light: #7fd1b9;
  --color-primary-dark: #2d8659;
  --color-secondary: #646cff;
  --color-secondary-light: #9d7cbf;
  --color-secondary-dark: #483f5d;
  --bg-color: #ffffff;
  --bg-color-secondary: #f8f8f8;
  --text-color-primary: #213547;
  --text-color-secondary: #476582;
  --border-color: #eaecef;
  --shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
}

// Темная тема
[data-theme="dark"] {
  --color-primary: #42d392;
  --color-primary-light: #74f0c8;
  --color-primary-dark: #2c9970;
  --color-secondary: #747bff;
  --color-secondary-light: #b0b0ff;
  --color-secondary-dark: #4d4db0;
  --bg-color: #1a1a1a;
  --bg-color-secondary: #2a2a2a;
  --text-color-primary: #eaeaea;
  --text-color-secondary: #a0a0a0;
  --border-color: #3a3a3a;
  --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
}
```

### 4. Пример компонента с использованием препроцессора

```vue
<template>
  <div :class="$style.card" :data-theme="currentTheme">
    <div :class="$style.header">
      <h3 :class="$style.title">{{ title }}</h3>
      <button 
        :class="$style.themeToggle" 
        @click="toggleTheme"
        aria-label="Переключить тему"
      >
        {{ currentTheme === 'light' ? '🌙' : '☀️' }}
      </button>
    </div>
    <div :class="$style.content">
      <p :class="$style.description">{{ description }}</p>
      <div :class="$style.actions">
        <button :class="$style.buttonPrimary">Действие 1</button>
        <button :class="$style.buttonSecondary">Действие 2</button>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  name: 'ThemedCard',
  props: {
    title: {
      type: String,
      required: true
    },
    description: {
      type: String,
      required: true
    }
  },
  data() {
    return {
      currentTheme: 'light'
    }
  },
  methods: {
    toggleTheme() {
      this.currentTheme = this.currentTheme === 'light' ? 'dark' : 'light';
      localStorage.setItem('theme', this.currentTheme);
    }
  },
  mounted() {
    const savedTheme = localStorage.getItem('theme');
    if (savedTheme) {
      this.currentTheme = savedTheme;
    }
  }
}
</script>

<style lang="scss" module>
@import '@/assets/scss/variables.scss';

.card {
  padding: $spacing-lg;
  background-color: var(--bg-color);
  color: var(--text-color-primary);
  border: 1px solid var(--border-color);
  border-radius: $border-radius-lg;
  box-shadow: var(--shadow);
  transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease;
  
  &:hover {
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
  }
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: $spacing-md;
}

.title {
  color: var(--color-primary);
  font-size: $font-size-xl;
  font-weight: $font-weight-semibold;
  margin: 0;
}

.themeToggle {
  background: none;
  border: none;
  font-size: 1.5rem;
  cursor: pointer;
  padding: $spacing-xs;
  border-radius: $border-radius;
  
  &:hover {
    background-color: var(--bg-color-secondary);
  }
}

.content {
  .description {
    color: var(--text-color-secondary);
    line-height: 1.6;
    margin-bottom: $spacing-md;
  }
}

.actions {
  display: flex;
  gap: $spacing-sm;
}

.buttonPrimary {
  background-color: var(--color-primary);
  color: white;
  border: none;
  padding: $spacing-sm $spacing-md;
  border-radius: $border-radius;
  cursor: pointer;
  font-weight: $font-weight-medium;
  transition: background-color 0.2s ease;
  
  &:hover {
    background-color: var(--color-primary-dark);
  }
}

.buttonSecondary {
  background-color: transparent;
  color: var(--color-secondary);
  border: 1px solid var(--color-secondary);
  padding: $spacing-sm $spacing-md;
  border-radius: $border-radius;
  cursor: pointer;
  font-weight: $font-weight-medium;
  transition: all 0.2s ease;
  
  &:hover {
    background-color: var(--color-secondary);
    color: white;
  }
}
</style>
```

## Адаптация под российские реалии

### Поддержка кириллических шрифтов

```scss
// src/assets/scss/base/_typography.scss

// Переменные для кириллических шрифтов
$font-family-cyrillic: 'Roboto', 'Arial Cyr', 'Helvetica Cyr', sans-serif;

body {
  font-family: $font-family-cyrillic;
  // Увеличенный интерлиньяж для кириллических шрифтов
  line-height: 1.7;
  // Небольшое увеличение интервала между буквами
  letter-spacing: 0.2px;
}

// Миксин для кириллической типографики
@mixin cyrillic-typography {
  font-family: $font-family-cyrillic;
  line-height: 1.7;
  letter-spacing: 0.2px;
}

.document-content {
  @include cyrillic-typography;
  font-size: 16px;
  
  h1, h2, h3, h4, h5, h6 {
    line-height: 1.3;
  }
  
  p {
    margin-bottom: 1em;
  }
}
```

### Учет требований доступности

```scss
// src/assets/scss/base/_accessibility.scss

// Минимальные размеры для интерактивных элементов
$min-tap-target: 44px;

// Цвета для фокуса
:focus {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

// Класс для скринридеров
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

// Кнопки с минимальным размером
.button {
  min-height: $min-tap-target;
  min-width: $min-tap-target;
  padding: 12px 20px;
  
  &.small {
    min-height: 36px;
    min-width: 36px;
    padding: 8px 12px;
  }
}

// Поддержка prefers-reduced-motion
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

### Локализация интерфейса

```scss
// src/assets/scss/base/_localization.scss

// Стили для разных направлений текста
[dir="rtl"] {
  text-align: right;
  
  .navigation {
    flex-direction: row-reverse;
  }
  
  .icon {
    transform: scaleX(-1);
  }
}

// Специфические стили для русского языка
:lang(ru) {
  font-family: 'Roboto', 'Arial', sans-serif;
  line-height: 1.7;
}

// Стили для уменьшения размера шрифта при длинных словах
.text-container {
  hyphens: auto;
  word-break: break-word;
}
```

## Лучшие практики

### 1. Использование семантических переменных

```scss
// Вместо цветов
$red: #e74c3c;
$blue: #3498db;

// Используйте семантические имена
$color-error: #e74c3c;
$color-primary: #3498db;
$color-success: #2ecc71;
$color-warning: #f39c12;
```

### 2. Модульная структура

```scss
// main.scss
@import 'abstracts/variables';
@import 'abstracts/mixins';
@import 'abstracts/functions';
@import 'base/reset';
@import 'base/typography';
@import 'base/utilities';
@import 'components/buttons';
@import 'components/forms';
@import 'layout/grid';
@import 'layout/navigation';
```

### 3. Документирование сложных миксинов

```scss
// Миксин для создания адаптивного текста
// Принимает минимальный и максимальный размеры шрифта
// и диапазон ширины экрана, в котором будет происходить масштабирование
// @param {Number} $min-size - Минимальный размер шрифта
// @param {Number} $max-size - Максимальный размер шрифта
// @param {Number} $min-screen - Минимальная ширина экрана
// @param {Number} $max-screen - Максимальная ширина экрана
@mixin responsive-font($min-size: 14px, $max-size: 18px, $min-screen: 320px, $max-screen: 1200px) {
  font-size: $min-size;
  
  @media (min-width: $min-screen) {
    font-size: calc(#{$min-size} + #{($max-size - $min-size)} * ((100vw - #{$min-screen}) / #{$max-screen - $min-screen}));
  }
  
  @media (min-width: $max-screen) {
    font-size: $max-size;
  }
}
```

## Сравнение препроцессоров

| Функция | Sass/SCSS | Less | Stylus |
|---------|-----------|------|--------|
| Переменные | `$variable` | `@variable` | `variable` |
| Вложенные правила | ✅ | ✅ | ✅ |
| Миксины | ✅ | ✅ | ✅ |
| Наследование | @extend | - | @extend |
| Математические операции | ✅ | ✅ | ✅ |
| Циклы | ✅ | ✅ | ✅ |
| Условия | @if/@else | .when() | if/else |
| Функции | ✅ | ✅ | ✅ |
| Поддержка в Vue | ✅ | ✅ | ✅ |

## Заключение

Препроцессоры CSS значительно расширяют возможности стилизации в Vue-приложениях, позволяя писать более структурированный, переиспользуемый и поддерживаемый код. Выбор препроцессора зависит от предпочтений команды разработчиков и конкретных требований проекта. При правильном использовании препроцессоры могут значительно улучшить качество и производительность разработки.

## См. также

- [[Скоуп-стили]]
- [[Глобальные-стили]]
- [[CSS-модули]]
- [[PostCSS]]
- [[Vue компоненты]]

## Источники

- [Официальная документация Sass](https://sass-lang.com/)
- [Документация Less](http://lesscss.org/)
- [Документация Stylus](http://stylus-lang.com/)
- [Vue.js Documentation - CSS Features](https://vuejs.org/api/sfc-css-features.html)
- [Sass Guidelines](https://sass-guidelin.es/)