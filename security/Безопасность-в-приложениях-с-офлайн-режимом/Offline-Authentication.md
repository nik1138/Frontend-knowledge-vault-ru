---
aliases: ["Оффлайн-аутентификация", "Автономная аутентификация", "Локальная аутентификация"]
tags: ["#security", "#authentication", "#offline", "#mobile-apps", "#local-storage", "#biometric"]
---

# Оффлайн-аутентификация в приложениях

## Введение в оффлайн-аутентификацию

Оффлайн-аутентификация (или автономная аутентификация) — это механизм, позволяющий пользователям проходить процесс аутентификации без подключения к интернету. В традиционных веб-приложениях аутентификация происходит на стороне сервера, где проверяются учетные данные пользователя. Однако в мобильных приложениях, десктопных приложениях и приложениях, работающих в условиях ограниченного подключения к интернету, требуется специальный подход для обеспечения безопасности и доступности функционала.

Оффлайн-аутентификация особенно важна в следующих сценариях:
- Мобильные приложения, работающие в удаленных районах
- Приложения для промышленного использования в условиях ограниченного подключения
- Приложения для автономного использования данных
- Приложения, требующие быстрого доступа к локальным ресурсам

## Зачем нужна оффлайн-аутентификация

Традиционная аутентификация требует постоянного подключения к серверу для проверки учетных данных. Однако в некоторых случаях это невозможно или нежелательно:

> [!note] Примечание
> Оффлайн-аутентификация позволяет обеспечить доступ к приложению даже при отсутствии интернет-соединения, что критично для приложений, работающих в автономном режиме.

### Основные причины использования оффлайн-аутентификации:

1. **Надежность**: Пользователи могут продолжать использовать приложение даже при проблемах с подключением
2. **Производительность**: Локальная проверка учетных данных быстрее, чем сетевой запрос
3. **Конфиденциальность**: Учетные данные не передаются по сети, снижая риск перехвата
4. **Комплаенс**: В некоторых отраслях данные не должны покидать локальное устройство

## Типы оффлайн-аутентификации

Существует несколько подходов к реализации оффлайн-аутентификации:

### 1. Локальная проверка учетных данных

Наиболее простой подход — хранение хэша пароля или других учетных данных на устройстве и их локальная проверка. Однако этот подход требует предварительной синхронизации учетных данных при первом подключении.

### 2. Токены с ограниченным сроком действия

Использование токенов (например, JWT), которые были получены при предыдущем подключении и сохранены локально. Эти токены могут быть проверены локально без обращения к серверу.

### 3. Предварительно синхронизированные учетные данные

В корпоративных приложениях может использоваться предварительная синхронизация учетных данных с сервера, что позволяет проверять их локально.

### 4. Биометрическая аутентификация

Использование биометрических данных пользователя (отпечатки пальцев, распознавание лица) для аутентификации без ввода пароля.

## Хранение учетных данных

Безопасное хранение учетных данных в оффлайн-режиме — критически важный аспект. Неправильное хранение может привести к компрометации учетных данных.

### Рекомендуемые подходы:

#### 1. Использование безопасного хранилища устройства

- **Android**: Keystore System
- **iOS**: Keychain Services
- **Windows**: DPAPI (Data Protection API)
- **macOS/Linux**: Secret Service API

#### 2. Шифрование учетных данных

Даже при использовании безопасного хранилища рекомендуется дополнительно шифровать чувствительные данные с использованием сильных криптографических алгоритмов.

#### 3. Хранение только хэшей

Вместо хранения паролей или токенов можно хранить только их хэши, что снижает риски при компрометации хранилища.

> [!warning] Важно
> Никогда не храните пароли в открытом виде в локальном хранилище. Всегда используйте шифрование или хэширование.

## Защита локальных токенов

При использовании токенов в оффлайн-режиме необходимо обеспечить их защиту от несанкционированного доступа:

### 1. Шифрование токенов

Токены должны быть зашифрованы с использованием сильных алгоритмов шифрования, таких как AES-256.

### 2. Ограничение срока действия

Даже в оффлайн-режиме токены должны иметь ограниченный срок действия, чтобы минимизировать последствия их компрометации.

### 3. Использование биометрической защиты

Доступ к токенам может быть защищен биометрической аутентификацией, что добавляет дополнительный уровень безопасности.

### 4. Периодическая проверка при подключении

При восстановлении подключения к интернету токены должны быть проверены на сервере и при необходимости обновлены или аннулированы.

## Биометрическая аутентификация

Биометрическая аутентификация — один из самых безопасных и удобных способов аутентификации в оффлайн-режиме. Она использует уникальные биологические характеристики пользователя для подтверждения личности.

### Преимущества биометрической аутентификации:

- **Удобство**: Пользователю не нужно запоминать пароли
- **Безопасность**: Биометрические данные трудно подделать
- **Офлайн-доступ**: Работает без подключения к интернету

### Технологии биометрической аутентификации:

- **Отпечатки пальцев**: Наиболее распространенная технология
- **Распознавание лица**: Используется в современных смартфонах
- **Сканирование радужной оболочки глаза**: Высокий уровень безопасности
- **Распознавание голоса**: Подходит для голосовых интерфейсов

> [!tip] Совет
> При реализации биометрической аутентификации всегда предоставляйте резервный метод аутентификации (например, PIN-код), на случай сбоя биометрического сенсора.

## Многофакторная аутентификация в оффлайн-режиме

Многофакторная аутентификация (MFA) в оффлайн-режиме представляет собой определенные сложности, так как традиционные методы MFA требуют подключения к серверу (например, SMS-коды или TOTP-токены).

### Возможные подходы к MFA в оффлайн-режиме:

#### 1. Предварительно сгенерированные коды

Пользователь может получить набор одноразовых кодов при последнем подключении, которые можно использовать в оффлайн-режиме.

#### 2. Синхронизированные TOTP-алгоритмы

Алгоритмы типа TOTP (Time-based One-Time Password) могут быть синхронизированы между устройством и сервером, позволяя генерировать коды без подключения.

#### 3. Комбинация локальных факторов

Использование комбинации локальных факторов: биометрия + PIN-код, что создает многофакторную аутентификацию без необходимости подключения к интернету.

#### 4. Аутентификация по местоположению

Использование GPS или других данных о местоположении как фактора аутентификации (требует осторожного подхода из-за приватности).

## Связанные файлы и концепции

- [[Authentication-Methods]] - Обзор различных методов аутентификации
- [[Token-Storage-Security]] - Безопасное хранение токенов
- [[Biometric-Security]] - Подробное рассмотрение биометрической безопасности
- [[Mobile-App-Security]] - Общие вопросы безопасности мобильных приложений
- [[JWT-Offline-Usage]] - Использование JWT-токенов в оффлайн-режиме
- [[Local-Data-Protection]] - Защита локальных данных в приложениях
- [[Security-Best-Practices]] - Лучшие практики безопасности
- [[Cryptography-Basics]] - Основы криптографии для разработчиков
- [[Secure-Session-Management]] - Безопасное управление сессиями
- [[Threat-Modeling]] - Моделирование угроз для приложений

## Заключение

Оффлайн-аутентификация — важный компонент современных приложений, особенно в мобильной и корпоративной среде. При правильной реализации она обеспечивает баланс между доступностью, удобством и безопасностью. Ключевые принципы успешной реализации:

- Использование безопасного хранилища устройства
- Шифрование всех чувствительных данных
- Интеграция биометрической аутентификации
- Регулярная проверка и обновление токенов при подключении
- Предоставление резервных методов аутентификации

> [!success] Ключевые выводы
> - Оффлайн-аутентификация необходима для приложений, работающих без постоянного подключения к интернету
> - Безопасное хранение учетных данных критично для предотвращения компрометации
> - Биометрическая аутентификация обеспечивает высокий уровень безопасности и удобства
> - Даже в оффлайн-режиме необходимо применять принципы многофакторной аутентификации

## Валидация локальных данных

При оффлайн-аутентификации критически важно обеспечить целостность и валидность локально хранящихся данных. Поскольку проверка не может быть выполнена на сервере, приложение должно самостоятельно контролировать качество и безопасность данных, используемых для аутентификации.

### Основные аспекты валидации:

1. **Проверка целостности данных**: Использование контрольных сумм или цифровых подписей для проверки того, что данные не были изменены
2. **Валидация формата учетных данных**: Проверка соответствия формату (например, длина пароля, структура токена)
3. **Проверка сроков действия**: Даже в оффлайн-режиме токены и учетные данные должны иметь ограниченный срок действия
4. **Контроль несанкционированного доступа**: Проверка того, что данные не были скомпрометированы (например, через root/jailbreak-дetection)

> [!tip] Совет
> Используйте механизмы контроля целостности, такие как HMAC, для проверки неизменности локальных данных аутентификации.

### Реализация валидации:

#### 1. Криптографические контрольные суммы

Для проверки целостности локальных данных можно использовать криптографические хэши или HMAC:

```javascript
// Пример проверки целостности локальных учетных данных
const validateIntegrity = (data, storedHmac) => {
  const computedHmac = crypto
    .createHmac('sha256', secretKey)
    .update(JSON.stringify(data))
    .digest('hex');
  
  return crypto.timingSafeEqual(
    Buffer.from(computedHmac),
    Buffer.from(storedHmac)
  );
};
```

#### 2. Проверка структуры данных

Важно проверять структуру локальных данных на соответствие ожидаемому формату:

```javascript
const validateCredentialsStructure = (credentials) => {
  return (
    credentials.hasOwnProperty('userId') &&
    credentials.hasOwnProperty('token') &&
    credentials.hasOwnProperty('expiresAt') &&
    typeof credentials.userId === 'string' &&
    typeof credentials.token === 'string' &&
    typeof credentials.expiresAt === 'number'
  );
};
```

#### 3. Проверка сроков действия

Даже в оффлайн-режиме необходимо проверять, не истек ли срок действия локальных токенов:

```javascript
const isTokenValid = (expiresAt) => {
  const now = Math.floor(Date.now() / 1000); // Текущее время в секундах
  return now < expiresAt;
};
```

## Синхронизация статуса аутентификации

При восстановлении подключения к интернету необходимо синхронизировать статус аутентификации между локальным устройством и сервером. Это включает проверку актуальности локальных данных и обновление состояния аутентификации при необходимости.

### Основные задачи синхронизации:

1. **Проверка актуальности локальных токенов**: Сервер должен подтвердить или отозвать локальные токены
2. **Обновление статуса пользователя**: Проверка, не был ли аккаунт заблокирован или удален
3. **Синхронизация изменений учетных данных**: Обновление локальных данных при изменении на сервере
4. **Обработка конфликтов**: Разрешение ситуаций, когда локальное и серверное состояние различаются

### Процесс синхронизации:

#### 1. Проверка токенов при подключении

При восстановлении подключения приложение должно проверить статус локальных токенов:

```javascript
const syncAuthStatus = async () => {
  try {
    const localToken = getLocalToken();
    const response = await fetch('/api/auth/sync', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${localToken}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        localTokenInfo: getLocalTokenInfo()
      })
    });
    
    const result = await response.json();
    
    if (result.status === 'invalid') {
      // Токен недействителен - очистить локальное состояние
      clearLocalAuthData();
      return false;
    } else if (result.status === 'expired') {
      // Требуется обновление токена
      return await refreshToken();
    } else if (result.status === 'valid') {
      // Токен действителен - обновить локальные данные
      updateLocalAuthData(result.userData);
      return true;
    }
  } catch (error) {
    console.error('Ошибка синхронизации статуса аутентификации:', error);
    return false;
  }
};
```

#### 2. Обработка статуса пользователя

При синхронизации важно проверить, не изменилось ли состояние аккаунта:

```javascript
const handleUserStatus = async (serverStatus) => {
  switch (serverStatus.accountStatus) {
    case 'active':
      // Аккаунт активен - обновить локальное состояние
      updateLocalAuthState('authenticated');
      break;
    case 'suspended':
      // Аккаунт приостановлен - очистить локальное состояние
      clearLocalAuthData();
      showAccountSuspendedMessage();
      break;
    case 'deleted':
      // Аккаунт удален - очистить все локальные данные
      clearAllLocalData();
      redirectToLogin();
      break;
    default:
      console.warn('Неизвестный статус аккаунта:', serverStatus.accountStatus);
  }
};
```

#### 3. Обновление локальных данных

При успешной синхронизации необходимо обновить локальные данные:

```javascript
const updateLocalAuthData = (serverData) => {
  // Обновить токены
  setLocalToken(serverData.accessToken);
  setLocalRefreshToken(serverData.refreshToken);
  
  // Обновить информацию о пользователе
  setLocalUserInfo(serverData.user);
  
  // Обновить права доступа
  setLocalPermissions(serverData.permissions);
  
  // Обновить сроки действия
  setLocalTokenExpiry(serverData.expiresAt);
};
```

## Управление сессиями

В оффлайн-режиме управление сессиями требует особого подхода, так как традиционные серверные механизмы недоступны. Приложение должно самостоятельно управлять жизненным циклом сессии на устройстве.

### Основные аспекты управления сессиями:

1. **Локальное хранение состояния сессии**: Сохранение информации о текущем состоянии аутентификации
2. **Таймеры и таймауты сессии**: Автоматическое завершение сессии по истечении времени бездействия
3. **Мониторинг активности пользователя**: Определение активности для продления сессии
4. **Безопасное завершение сессии**: Корректная очистка данных при выходе

### Реализация управления сессиями:

#### 1. Локальное хранение состояния

Состояние сессии должно храниться в безопасном хранилище:

```javascript
class SessionManager {
  constructor() {
    this.sessionTimeout = 30 * 60 * 1000; // 30 минут
    this.inactivityTimeout = 15 * 60 * 1000; // 15 минут бездействия
    this.lastActivity = Date.now();
    this.sessionTimer = null;
    this.inactivityTimer = null;
  }

  // Инициализация сессии
  initSession(userData, token) {
    const sessionData = {
      userId: userData.id,
      userName: userData.name,
      token: token,
      createdAt: Date.now(),
      lastActivity: Date.now(),
      isActive: true
    };

    // Сохранить в безопасное хранилище
    this.saveToSecureStorage('session', sessionData);
    
    // Запустить таймеры
    this.startTimers();
  }

  // Проверка активности сессии
  isSessionActive() {
    const sessionData = this.getFromSecureStorage('session');
    if (!sessionData || !sessionData.isActive) {
      return false;
    }

    const now = Date.now();
    return (now - sessionData.lastActivity) < this.sessionTimeout;
  }

  // Обновление времени активности
  updateActivity() {
    const sessionData = this.getFromSecureStorage('session');
    if (sessionData) {
      sessionData.lastActivity = Date.now();
      this.saveToSecureStorage('session', sessionData);
      this.lastActivity = sessionData.lastActivity;
    }
  }

  // Запуск таймеров
  startTimers() {
    this.clearTimers();
    
    // Таймер сессии
    this.sessionTimer = setTimeout(() => {
      this.endSession();
    }, this.sessionTimeout);

    // Таймер бездействия
    this.inactivityTimer = setInterval(() => {
      const now = Date.now();
      if ((now - this.lastActivity) > this.inactivityTimeout) {
        this.endSession();
      }
    }, 60000); // Проверять каждую минуту
  }

  // Очистка таймеров
  clearTimers() {
    if (this.sessionTimer) {
      clearTimeout(this.sessionTimer);
      this.sessionTimer = null;
    }
    if (this.inactivityTimer) {
      clearInterval(this.inactivityTimer);
      this.inactivityTimer = null;
    }
  }

  // Завершение сессии
  endSession() {
    this.clearTimers();
    this.clearSecureStorage('session');
    this.onSessionEnd();
  }
}
```

#### 2. Мониторинг активности пользователя

Для определения активности пользователя можно отслеживать события:

```javascript
// Инициализация мониторинга активности
const setupActivityMonitoring = (sessionManager) => {
  const events = ['mousedown', 'mousemove', 'keypress', 'scroll', 'touchstart', 'click'];
  
  const updateActivity = () => {
    sessionManager.updateActivity();
  };

  events.forEach(event => {
    document.addEventListener(event, updateActivity, true);
  });

  // Также отслеживать фокус окна
  window.addEventListener('focus', updateActivity);
  window.addEventListener('blur', updateActivity);
};
```

#### 3. Безопасное завершение сессии

При завершении сессии необходимо корректно очистить все данные:

```javascript
const secureSessionEnd = async () => {
  // Очистить токены
  await clearSecureTokens();
  
  // Очистить информацию о пользователе
  clearLocalUserInfo();
  
  // Очистить права доступа
  clearLocalPermissions();
  
  // Очистить все временные данные
  clearTempData();
  
  // Обновить UI
  updateUIForLoggedOutState();
  
  // Перенаправить на страницу входа
  redirectToLogin();
};
```

## Обновление учетных данных

В оффлайн-режиме обновление учетных данных представляет определенные сложности, так как изменения не могут быть сразу синхронизированы с сервером. Необходимо реализовать механизмы для безопасного обновления и последующей синхронизации данных.

### Основные аспекты обновления:

1. **Локальное обновление**: Возможность изменения учетных данных на устройстве
2. **Буферизированные изменения**: Хранение изменений для последующей синхронизации
3. **Валидация изменений**: Проверка корректности новых данных
4. **Обработка конфликтов**: Разрешение ситуаций при синхронизации

### Реализация обновления учетных данных:

#### 1. Буферизированные изменения

Для хранения изменений можно использовать специальную очередь:

```javascript
class CredentialUpdateManager {
  constructor() {
    this.updateQueue = [];
  }

  // Добавить изменение в очередь
  addUpdate(update) {
    const updateRecord = {
      id: generateUUID(),
      type: update.type, // 'password', 'profile', 'preferences'
      data: update.data,
      timestamp: Date.now(),
      synced: false
    };

    this.updateQueue.push(updateRecord);
    this.saveQueue();
  }

  // Сохранить очередь в безопасное хранилище
  saveQueue() {
    this.saveToSecureStorage('credentialUpdates', this.updateQueue);
  }

  // Загрузить очередь из безопасного хранилища
  loadQueue() {
    const savedQueue = this.getFromSecureStorage('credentialUpdates');
    if (savedQueue) {
      this.updateQueue = savedQueue;
    }
  }

  // Отправить изменения при подключении
  async syncUpdates() {
    const unsyncedUpdates = this.updateQueue.filter(u => !u.synced);
    
    for (const update of unsyncedUpdates) {
      try {
        const response = await fetch('/api/auth/update', {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${getLocalToken()}`,
            'Content-Type': 'application/json'
          },
          body: JSON.stringify(update)
        });

        if (response.ok) {
          update.synced = true;
          this.saveQueue();
        } else {
          throw new Error(`Failed to sync update ${update.id}`);
        }
      } catch (error) {
        console.error('Ошибка синхронизации обновления:', error);
        // Можем добавить логику повторных попыток
      }
    }
  }
}
```

#### 2. Валидация изменений

Перед обновлением необходимо проверить корректность новых данных:

```javascript
const validateCredentialUpdate = (update) => {
  switch (update.type) {
    case 'password':
      return validatePassword(update.data.newPassword);
    case 'profile':
      return validateProfileData(update.data);
    case 'email':
      return validateEmail(update.data.newEmail);
    default:
      return false;
  }
};

const validatePassword = (password) => {
  // Проверка сложности пароля
  const minLength = 8;
  const hasUpperCase = /[A-Z]/.test(password);
  const hasLowerCase = /[a-z]/.test(password);
  const hasNumbers = /\d/.test(password);
  const hasSpecialChar = /[!@#$%^&*(),.?":{}|<>]/.test(password);

  return password.length >= minLength && 
         hasUpperCase && 
         hasLowerCase && 
         hasNumbers && 
         hasSpecialChar;
};

const validateProfileData = (profileData) => {
  // Проверка обязательных полей
  return profileData.hasOwnProperty('firstName') &&
         profileData.hasOwnProperty('lastName') &&
         typeof profileData.firstName === 'string' &&
         typeof profileData.lastName === 'string' &&
         profileData.firstName.trim().length > 0 &&
         profileData.lastName.trim().length > 0;
};
```

#### 3. Обработка конфликтов

При синхронизации могут возникнуть конфликты, которые нужно корректно обработать:

```javascript
const handleSyncConflict = async (localUpdate, serverData) => {
  switch (localUpdate.type) {
    case 'profile':
      // Объединить профили, приоритет у последнего изменения
      const mergedProfile = mergeProfiles(serverData.profile, localUpdate.data, localUpdate.timestamp);
      return await updateServerProfile(mergedProfile);
      
    case 'password':
      // В случае конфликта паролей - запросить повторный ввод
      return await requestPasswordReentry();
      
    case 'preferences':
      // Объединить настройки
      const mergedPreferences = { ...serverData.preferences, ...localUpdate.data };
      return await updateServerPreferences(mergedPreferences);
      
    default:
      throw new Error(`Неизвестный тип обновления: ${localUpdate.type}`);
  }
};
```

## Обработка сбоев аутентификации

Обработка сбоев аутентификации в оффлайн-режиме требует особого внимания, так как стандартные механизмы серверной аутентификации недоступны. Необходимо реализовать надежные механизмы обработки ошибок и восстановления.

### Типы сбоев аутентификации:

1. **Неверные учетные данные**: Пользователь ввел неправильный пароль или другие данные
2. **Поврежденные локальные данные**: Локальные учетные данные были повреждены или скомпрометированы
3. **Сбои биометрической аутентификации**: Ошибка сканера отпечатков или распознавания лица
4. **Истечение срока действия токенов**: Локальные токены устарели
5. **Проблемы с безопасным хранилищем**: Ошибка доступа к защищенному хранилищу

### Реализация обработки сбоев:

#### 1. Обработка неверных учетных данных

```javascript
const handleInvalidCredentials = () => {
  const attempts = getLocalAuthAttempts();
  const maxAttempts = 5;
  
  if (attempts >= maxAttempts) {
    // Блокировка на определенное время
    const lockoutTime = Date.now() + (15 * 60 * 1000); // 15 минут
    setLocalLockout(lockoutTime);
    showLockoutMessage(15);
  } else {
    // Увеличить счетчик попыток
    setLocalAuthAttempts(attempts + 1);
    showInvalidCredentialsMessage(maxAttempts - attempts);
  }
};

// Сброс попыток при успешной аутентификации
const resetAuthAttempts = () => {
  setLocalAuthAttempts(0);
  clearLocalLockout();
};
```

#### 2. Обработка поврежденных данных

```javascript
const handleCorruptedData = async () => {
  console.error('Обнаружены поврежденные локальные данные аутентификации');
  
  // Очистить все локальные данные
  await clearAllLocalAuthData();
  
  // Попытаться восстановить из резервной копии
  const backupData = await getAuthBackup();
  if (backupData && validateBackupData(backupData)) {
    await restoreAuthData(backupData);
    showRecoverySuccessMessage();
  } else {
    // Требовать повторную аутентификацию при подключении
    setRequiresReauth(true);
    showRecoveryRequiredMessage();
  }
};

const validateBackupData = (backup) => {
  // Проверить целостность и валидность резервных данных
  return backup.hasOwnProperty('integrityCheck') && 
         backup.hasOwnProperty('timestamp') && 
         verifyIntegrity(backup);
};
```

#### 3. Обработка сбоев биометрии

```javascript
const handleBiometricFailure = (error) => {
  console.error('Сбой биометрической аутентификации:', error);
  
  switch (error.code) {
    case 'BIOMETRIC_ERROR_NO_HARDWARE':
      showBiometricNotAvailableMessage();
      break;
    case 'BIOMETRIC_ERROR_HW_UNAVAILABLE':
      showBiometricUnavailableMessage();
      break;
    case 'BIOMETRIC_ERROR_NONE_ENROLLED':
      showBiometricNotEnrolledMessage();
      break;
    case 'BIOMETRIC_ERROR_LOCKOUT':
      showBiometricLockoutMessage();
      break;
    default:
      // Предложить резервный метод аутентификации
      showFallbackAuthOption();
  }
};
```

#### 4. Обработка истечения токенов

```javascript
const handleTokenExpiration = async () => {
  // Попытаться обновить токен, если возможно
  if (hasRefreshToken()) {
    try {
      const success = await refreshTokenOffline();
      if (success) {
        return true;
      }
    } catch (error) {
      console.error('Не удалось обновить токен:', error);
    }
  }
  
  // Если обновить не удалось - требовать повторную аутентификацию
  clearLocalAuthData();
  showTokenExpiredMessage();
  return false;
};

// Попытка обновления токена без подключения
const refreshTokenOffline = async () => {
  const refreshToken = getLocalRefreshToken();
  const userId = getLocalUserId();
  
  // Проверить, не истек ли refresh token
  if (isTokenExpired(refreshToken)) {
    return false;
  }
  
  // Генерация нового токена на основе refresh token (если поддерживается)
  // В реальном приложении это требует специальной реализации
  const newToken = generateOfflineToken(userId);
  if (newToken) {
    setLocalToken(newToken);
    return true;
  }
  
  return false;
};
```

## Безопасность ввода паролей

Безопасность ввода паролей в оффлайн-режиме особенно важна, так как любые уязвимости могут привести к компрометации локальных учетных данных. Необходимо реализовать надежные механизмы защиты ввода и обработки паролей.

### Основные аспекты безопасности ввода:

1. **Защита от keyloggers**: Предотвращение перехвата ввода пароля
2. **Визуальная защита**: Скрытие пароля от посторонних глаз
3. **Безопасное хранение временных данных**: Защита введенных данных в памяти
4. **Анти-форсинг**: Защита от подбора пароля

### Реализация защиты ввода паролей:

#### 1. Защита от keyloggers

```javascript
// Использование виртуальной клавиатуры для защиты от keyloggers
class SecurePasswordInput {
  constructor(inputElement) {
    this.inputElement = inputElement;
    this.virtualKeyboard = this.createVirtualKeyboard();
    this.setupEventHandlers();
  }

  createVirtualKeyboard() {
    const keyboard = document.createElement('div');
    keyboard.className = 'secure-virtual-keyboard';
    keyboard.style.display = 'none';
    
    // Создать кнопки клавиатуры
    const keys = [
      '1', '2', '3', '4', '5', '6', '7', '8', '9', '0',
      'q', 'w', 'e', 'r', 't', 'y', 'u', 'i', 'o', 'p',
      'a', 's', 'd', 'f', 'g', 'h', 'j', 'k', 'l',
      'z', 'x', 'c', 'v', 'b', 'n', 'm', 'Backspace'
    ];

    keys.forEach(key => {
      const button = document.createElement('button');
      button.textContent = key;
      button.onclick = () => this.handleKeyPress(key);
      keyboard.appendChild(button);
    });

    document.body.appendChild(keyboard);
    return keyboard;
  }

  handleKeyPress(key) {
    if (key === 'Backspace') {
      this.currentPassword = this.currentPassword.slice(0, -1);
    } else {
      this.currentPassword += key;
    }
    
    // Обновить скрытое поле ввода
    this.inputElement.value = this.currentPassword;
  }

  setupEventHandlers() {
    this.inputElement.addEventListener('focus', () => {
      this.showVirtualKeyboard();
    });

    this.inputElement.addEventListener('blur', () => {
      this.hideVirtualKeyboard();
    });
  }

  showVirtualKeyboard() {
    this.virtualKeyboard.style.display = 'grid';
  }

  hideVirtualKeyboard() {
    this.virtualKeyboard.style.display = 'none';
  }
}
```

#### 2. Визуальная защита

```javascript
// Добавить защиту экрана от посторонних глаз
const setupScreenProtection = () => {
  let isPasswordVisible = false;
  const toggleButton = document.getElementById('password-visibility-toggle');
  
  toggleButton.addEventListener('click', () => {
    const passwordInput = document.getElementById('password');
    if (isPasswordVisible) {
      passwordInput.type = 'password';
      toggleButton.textContent = '👁️';
    } else {
      passwordInput.type = 'text';
      toggleButton.textContent = '🙈';
    }
    isPasswordVisible = !isPasswordVisible;
  });

  // Добавить защиту от скриншотов
  document.addEventListener('visibilitychange', () => {
    if (document.visibilityState === 'visible') {
      // Восстановить поля ввода
      showPasswordFields();
    } else {
      // Скрыть поля ввода при сворачивании
      hidePasswordFields();
    }
  });
};
```

#### 3. Безопасное хранение в памяти

```javascript
// Использование безопасного хранения пароля в памяти
class SecurePasswordMemory {
  constructor() {
    this.passwordBuffer = null;
  }

  // Установить пароль в безопасный буфер
  setPassword(password) {
    // Очистить предыдущий буфер
    this.clearPassword();
    
    // Создать новый буфер и скопировать пароль
    this.passwordBuffer = new Uint8Array(password.length);
    for (let i = 0; i < password.length; i++) {
      this.passwordBuffer[i] = password.charCodeAt(i);
    }
  }

  // Получить хэш пароля без раскрытия оригинала
  getPasswordHash() {
    if (!this.passwordBuffer) {
      return null;
    }
    
    // Создать хэш из буфера
    const passwordString = String.fromCharCode.apply(null, this.passwordBuffer);
    return crypto.subtle.digest('SHA-256', new TextEncoder().encode(passwordString));
  }

  // Очистить буфер
  clearPassword() {
    if (this.passwordBuffer) {
      // Заполнить буфер случайными значениями перед очисткой
      for (let i = 0; i < this.passwordBuffer.length; i++) {
        this.passwordBuffer[i] = Math.floor(Math.random() * 256);
      }
      this.passwordBuffer = null;
    }
  }

  // Уничтожить объект при завершении
  destroy() {
    this.clearPassword();
  }
}
```

#### 4. Анти-форсинг

```javascript
const setupAntiBruteforce = () => {
  let attempts = 0;
  let lockoutUntil = 0;
  
  const checkBruteforceAttempt = () => {
    const now = Date.now();
    
    if (now < lockoutUntil) {
      const remaining = Math.ceil((lockoutUntil - now) / 1000);
      showLockoutMessage(remaining);
      return false;
    }
    
    attempts++;
    
    if (attempts >= 5) {
      // Заблокировать на 15 минут
      lockoutUntil = now + (15 * 60 * 1000);
      attempts = 0;
      showLockoutMessage(900);
      return false;
    }
    
    return true;
  };
  
  const resetAttempts = () => {
    attempts = 0;
    lockoutUntil = 0;
  };
  
  return { checkBruteforceAttempt, resetAttempts };
};
```

## Лучшие практики

При реализации оффлайн-аутентификации необходимо следовать определенным лучшим практикам для обеспечения безопасности и надежности системы.

### 1. Использование безопасного хранилища

Всегда используйте безопасное хранилище устройства для хранения чувствительных данных:
- Android: Keystore System
- iOS: Keychain Services
- Windows: DPAPI
- macOS/Linux: Secret Service API

> [!tip] Совет
> Даже при использовании безопасного хранилища рекомендуется дополнительно шифровать чувствительные данные с использованием сильных криптографических алгоритмов.

### 2. Минимизация хранящихся данных

Храните только минимально необходимый набор данных для аутентификации:
- Храните только хэши паролей, а не сами пароли
- Используйте короткоживущие токены
- Удаляйте неиспользуемые данные

### 3. Регулярная проверка целостности

Реализуйте механизмы проверки целостности локальных данных:
- Используйте HMAC для проверки неизменности данных
- Периодически проверяйте структуру и формат данных
- Обнаруживайте и обрабатывайте поврежденные данные

### 4. Обработка ошибок

Реализуйте надежную обработку ошибок:
- Обрабатывайте сбои аутентификации
- Обеспечьте резервные методы аутентификации
- Обрабатывайте сбои биометрической аутентификации

### 5. Защита от атак

Реализуйте защиту от различных типов атак:
- Защита от подбора пароля (anti-bruteforce)
- Защита от keyloggers (виртуальная клавиатура)
- Защита от фишинга (проверка подлинности интерфейса)

### 6. Синхронизация и обновление

Обеспечьте надежную синхронизацию данных при подключении:
- Проверяйте актуальность локальных данных
- Обновляйте учетные данные при необходимости
- Обрабатывайте конфликты синхронизации

### 7. Валидация ввода

Строго валидируйте все вводимые данные:
- Проверяйте формат и структуру данных
- Проверяйте сложность паролей
- Ограничивайте длину и типы вводимых данных

### 8. Мониторинг и аудит

Реализуйте локальный мониторинг и аудит:
- Логируйте попытки аутентификации
- Отслеживайте подозрительную активность
- Обеспечьте возможность аудита безопасности

### 9. Обновление и обслуживание

Регулярно обновляйте систему аутентификации:
- Обновляйте криптографические алгоритмы
- Исправляйте уязвимости
- Адаптируйте к новым угрозам

## Связанные файлы и концепции

- [[Authentication-Methods]] - Обзор различных методов аутентификации
- [[Token-Storage-Security]] - Безопасное хранение токенов
- [[Biometric-Security]] - Подробное рассмотрение биометрической безопасности
- [[Mobile-App-Security]] - Общие вопросы безопасности мобильных приложений
- [[JWT-Offline-Usage]] - Использование JWT-токенов в оффлайн-режиме
- [[Local-Data-Protection]] - Защита локальных данных в приложениях
- [[Security-Best-Practices]] - Лучшие практики безопасности
- [[Cryptography-Basics]] - Основы криптографии для разработчиков
- [[Secure-Session-Management]] - Безопасное управление сессиями
- [[Threat-Modeling]] - Моделирование угроз для приложений
- [[Password-Security]] - Безопасность паролей и управление ими
- [[Secure-Input-Methods]] - Безопасные методы ввода данных
- [[Offline-Data-Synchronization]] - Синхронизация данных в оффлайн-режиме
- [[Device-Security]] - Безопасность устройств и платформ
- [[Encryption-Methods]] - Методы шифрования для приложений
- [[Secure-Communication]] - Безопасная коммуникация в приложениях
- [[User-Privacy]] - Защита конфиденциальности пользователей
- [[Access-Control]] - Контроль доступа в приложениях
- [[Security-Auditing]] - Аудит безопасности приложений
- [[Vulnerability-Assessment]] - Оценка уязвимостей приложений