---
tags: [programming, regexp, flags, frontend, javascript]
aliases: [флаги-регулярных-выражений, regexp-flags]
---

# Флаги регулярных выражений

Флаги в регулярных выражениях изменяют поведение поиска и сопоставления. В JavaScript доступно несколько флагов, каждый из которых влияет на то, как движок регулярных выражений обрабатывает паттерн. Понимание флагов критически важно для эффективного использования регулярных выражений в веб-разработке.

## Основные флаги

### `g` (Global) - Глобальный поиск

Флаг `g` позволяет находить все совпадения в строке, а не только первое.

```javascript
const text = "JavaScript - это язык программирования. Язык JavaScript мощный.";
const regex = /JavaScript/; // Без флага g
const globalRegex = /JavaScript/g; // С флагом g

console.log(text.match(regex)); // ['JavaScript', index: 0, input: '...', groups: undefined]
console.log(text.match(globalRegex)); // ['JavaScript', 'JavaScript']
```

### `i` (IgnoreCase) - Регистронезависимый поиск

Флаг `i` позволяет игнорировать регистр при поиске.

```javascript
const text = "JavaScript, javascript, JAVASCRIPT";
const caseSensitiveRegex = /javascript/;
const caseInsensitiveRegex = /javascript/i;

console.log(text.match(caseSensitiveRegex)); // ['javascript'] - только одно совпадение
console.log(text.match(caseInsensitiveRegex)); // ['JavaScript'] - первое совпадение
console.log(text.match(/javascript/gi)); // ['JavaScript', 'javascript', 'JAVASCRIPT'] - все совпадения без учета регистра
```

### `m` (Multiline) - Многострочный режим

Флаг `m` изменяет поведение `^` и `$`, чтобы они соответствовали началу и концу каждой строки, а не всего текста.

```javascript
const multilineText = `Первая строка
Вторая строка
Третья строка`;

const regex = /^Вторая/gm; // Ищем "Вторая" в начале любой строки

console.log(multilineText.match(regex)); // ['Вторая']
```

## Современные флаги

### `s` (DotAll) - Режим DotAll

Флаг `s` (появился в ES2018) позволяет точке `.` соответствовать символу новой строки.

```javascript
const text = `Много
строчный
текст`;

const regex = /Много.*текст/; // Без флага s - не найдет совпадений
const dotAllRegex = /Много.*текст/s; // С флагом s - найдет совпадение

console.log(regex.test(text)); // false
console.log(dotAllRegex.test(text)); // true
```

### `u` (Unicode) - Поддержка Unicode

Флаг `u` включает полноценную поддержку Unicode, что важно для работы с эмодзи и другими символами.

```javascript
const emojiText = "Привет! 👋 Как дела?";
const basicRegex = /./g;
const unicodeRegex = /./gu;

console.log(emojiText.match(basicRegex).length); // 18 (неправильно считает эмодзи)
console.log(emojiText.match(unicodeRegex).length); // 17 (правильно считает эмодзи как один символ)
```

### `y` (Sticky) - Прилипающий поиск

Флаг `y` заставляет регулярное выражение искать совпадения только в позиции, указанной свойством `lastIndex`.

```javascript
const text = "JavaScript - мощный язык";
const stickyRegex = /Java/y;
const globalRegex = /Java/g;

stickyRegex.lastIndex = 0;
console.log(stickyRegex.test(text)); // true

stickyRegex.lastIndex = 2;
console.log(stickyRegex.test(text)); // false - совпадение не в позиции 2

console.log(globalRegex.test(text)); // true - глобальный поиск не зависит от позиции
```

## Практическое применение флагов в веб-разработке

### Валидация email с флагом i

```javascript
// Регистронезависимая проверка домена
function validateEmail(email) {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/i;
  return emailRegex.test(email);
}

console.log(validateEmail("USER@EXAMPLE.COM")); // true
console.log(validateEmail("user@example.com")); // true
```

### Многострочная валидация с флагом m

```javascript
// Проверка формата многострочного комментария
function validateMultilineComment(comment) {
  // Комментарии должны начинаться с "//" в начале строки
  const commentRegex = /^\/\/\s.*/gm;
  const matches = comment.match(commentRegex);
  
  return matches && matches.length > 0;
}

const code = `function test() {
  // Это комментарий
  console.log('Привет');
  // Еще один комментарий
}`;
console.log(validateMultilineComment(code)); // ['// Это комментарий', '// Еще один комментарий']
```

### Извлечение данных с флагом g

```javascript
// Извлечение всех email-адресов из текста
function extractEmails(text) {
  const emailRegex = /\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b/g;
  return text.match(emailRegex) || [];
}

const textWithMails = "Свяжитесь с нами: info@example.com или support@company.org";
console.log(extractEmails(textWithMails)); // ['info@example.com', 'support@company.org']
```

### Работа с юникодом и эмодзи

```javascript
// Подсчет слов в строке с эмодзи
function countWords(text) {
  const wordRegex = /\p{L}+/gu; // \p{L} соответствует любому буквенном символу в Unicode
  const matches = text.match(wordRegex);
  return matches ? matches.length : 0;
}

const textWithEmoji = "Привет! 👋 Как дела? 😊";
console.log(countWords(textWithEmoji)); // 4 слова
```

## Комбинация флагов

Флаги можно комбинировать в любом порядке:

```javascript
// Комбинация нескольких флагов
const regex = /pattern/gim; // глобальный, регистронезависимый, многострочный
const anotherRegex = new RegExp('pattern', 'misg'); // тот же результат, другой порядок

// Практический пример: извлечение всех URL из текста с разным регистром
function extractUrls(text) {
  const urlRegex = /https?:\/\/[^\s<>"{}|\\^`\[\]]+/gi;
  return text.match(urlRegex) || [];
}

const textWithUrls = `Посетите https://Example.com или http://test.ru для получения информации.
Также можно зайти на https://API.EXAMPLE.ORG`;
console.log(extractUrls(textWithUrls));
// ['https://Example.com', 'http://test.ru', 'https://API.EXAMPLE.ORG']
```

## Работа с флагами программно

```javascript
// Проверка установленных флагов
const regex = /test/gim;

console.log(regex.global);     // true
console.log(regex.ignoreCase); // true
console.log(regex.multiline);  // true
console.log(regex.sticky);     // false
console.log(regex.unicode);    // false
console.log(regex.dotAll);     // false

// Создание регулярного выражения с динамическими флагами
function createRegex(pattern, flags) {
  return new RegExp(pattern, flags);
}

const dynamicRegex = createRegex('test', 'gi');
console.log(dynamicRegex.flags); // 'gi'
```

## Практические примеры

### Форматирование текста с использованием флагов

```javascript
// Замена всех вхождений с учетом регистра
function replaceCaseInsensitive(text, search, replacement) {
  const regex = new RegExp(search, 'gi');
  return text.replace(regex, replacement);
}

const text = "JavaScript - это JavaScript, а не javascript!";
console.log(replaceCaseInsensitive(text, 'javascript', 'TypeScript'));
// "TypeScript - это TypeScript, а не TypeScript!"
```

### Валидация и очистка пользовательского ввода

```javascript
// Удаление всех HTML-тегов из строки
function stripHtmlTags(html) {
  const tagRegex = /<[^>]*>/gs; // Флаги g и s для глобального поиска и DotAll
  return html.replace(tagRegex, '');
}

const htmlString = `<p>Это <strong>жирный</strong> текст</p>
<div>И это <em>курсив</em></div>`;
console.log(stripHtmlTags(htmlString));
// "Это жирный текст\nИ это курсив"
```

## Следующие шаги

После изучения флагов рекомендуется перейти к:

- [[Синтаксис регулярных выражений]]
- [[Продвинутые темы]]
- [[Практические примеры]]