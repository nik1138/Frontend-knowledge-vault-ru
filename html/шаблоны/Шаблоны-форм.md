---
aliases: ["Шаблоны форм", "HTML формы", "Формы веб-сайта"]
tags: [html, шаблоны, формы, веб-разработка, валидация]
---

# Шаблоны форм

## Общие шаблоны HTML-форм

HTML-формы являются важной частью интерактивных веб-сайтов, позволяя пользователям отправлять данные на сервер. В 2025 году в российской веб-разработке особое внимание уделяется безопасности, доступности и пользовательскому опыту при создании форм.

## Базовая форма регистрации

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Регистрация - Название сайта</title>
    <link rel="stylesheet" href="styles/forms.css">
</head>
<body>
    <main>
        <div class="form-container">
            <h1>Регистрация</h1>
            
            <form id="registration-form" action="/register" method="POST">
                <div class="form-group">
                    <label for="first-name">Имя</label>
                    <input 
                        type="text" 
                        id="first-name" 
                        name="first_name" 
                        required 
                        minlength="2" 
                        maxlength="50"
                        aria-describedby="first-name-error"
                    >
                    <div id="first-name-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group">
                    <label for="last-name">Фамилия</label>
                    <input 
                        type="text" 
                        id="last-name" 
                        name="last_name" 
                        required 
                        minlength="2" 
                        maxlength="50"
                        aria-describedby="last-name-error"
                    >
                    <div id="last-name-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group">
                    <label for="email">Email</label>
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        required 
                        aria-describedby="email-error"
                    >
                    <div id="email-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group">
                    <label for="phone">Телефон</label>
                    <input 
                        type="tel" 
                        id="phone" 
                        name="phone" 
                        required 
                        pattern="[0-9]{11}"
                        aria-describedby="phone-error"
                    >
                    <div id="phone-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group">
                    <label for="password">Пароль</label>
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        required 
                        minlength="8"
                        aria-describedby="password-error"
                    >
                    <div id="password-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group">
                    <label for="confirm-password">Подтверждение пароля</label>
                    <input 
                        type="password" 
                        id="confirm-password" 
                        name="confirm_password" 
                        required 
                        aria-describedby="confirm-password-error"
                    >
                    <div id="confirm-password-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group checkbox-group">
                    <input 
                        type="checkbox" 
                        id="terms" 
                        name="terms" 
                        required
                        aria-describedby="terms-error"
                    >
                    <label for="terms">Я согласен с <a href="/terms">условиями использования</a> и <a href="/privacy">политикой конфиденциальности</a></label>
                    <div id="terms-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group checkbox-group">
                    <input 
                        type="checkbox" 
                        id="newsletter" 
                        name="newsletter"
                    >
                    <label for="newsletter">Получать новости и специальные предложения</label>
                </div>
                
                <button type="submit" class="btn btn-primary">Зарегистрироваться</button>
            </form>
        </div>
    </main>

    <script>
        // Пример клиентской валидации
        document.getElementById('registration-form').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Сброс ошибок
            document.querySelectorAll('.error-message').forEach(el => el.textContent = '');
            
            // Получение значений полей
            const firstName = document.getElementById('first-name').value.trim();
            const lastName = document.getElementById('last-name').value.trim();
            const email = document.getElementById('email').value.trim();
            const phone = document.getElementById('phone').value.replace(/\D/g, '');
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirm-password').value;
            const terms = document.getElementById('terms').checked;
            
            let isValid = true;
            
            // Валидация имени
            if (!firstName) {
                document.getElementById('first-name-error').textContent = 'Пожалуйста, введите имя';
                isValid = false;
            } else if (firstName.length < 2) {
                document.getElementById('first-name-error').textContent = 'Имя должно содержать не менее 2 символов';
                isValid = false;
            }
            
            // Валидация фамилии
            if (!lastName) {
                document.getElementById('last-name-error').textContent = 'Пожалуйста, введите фамилию';
                isValid = false;
            } else if (lastName.length < 2) {
                document.getElementById('last-name-error').textContent = 'Фамилия должна содержать не менее 2 символов';
                isValid = false;
            }
            
            // Валидация email
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!email) {
                document.getElementById('email-error').textContent = 'Пожалуйста, введите email';
                isValid = false;
            } else if (!emailRegex.test(email)) {
                document.getElementById('email-error').textContent = 'Пожалуйста, введите корректный email';
                isValid = false;
            }
            
            // Валидация телефона
            if (!phone) {
                document.getElementById('phone-error').textContent = 'Пожалуйста, введите телефон';
                isValid = false;
            } else if (phone.length !== 11) {
                document.getElementById('phone-error').textContent = 'Телефон должен содержать 11 цифр';
                isValid = false;
            }
            
            // Валидация пароля
            if (!password) {
                document.getElementById('password-error').textContent = 'Пожалуйста, введите пароль';
                isValid = false;
            } else if (password.length < 8) {
                document.getElementById('password-error').textContent = 'Пароль должен содержать не менее 8 символов';
                isValid = false;
            }
            
            // Валидация подтверждения пароля
            if (password !== confirmPassword) {
                document.getElementById('confirm-password-error').textContent = 'Пароли не совпадают';
                isValid = false;
            }
            
            // Валидация согласия с условиями
            if (!terms) {
                document.getElementById('terms-error').textContent = 'Необходимо согласиться с условиями';
                isValid = false;
            }
            
            // Если все валидации пройдены, отправляем форму
            if (isValid) {
                // Здесь можно выполнить отправку формы
                console.log('Форма регистрации валидна, отправляем данные...');
                // this.submit(); // Раскомментируйте для реальной отправки
            }
        });
    </script>
</body>
</html>
```

## Форма обратной связи

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Обратная связь - Название сайта</title>
    <link rel="stylesheet" href="styles/forms.css">
</head>
<body>
    <main>
        <div class="form-container">
            <h1>Обратная связь</h1>
            
            <form id="contact-form" action="/contact" method="POST">
                <div class="form-group">
                    <label for="contact-name">Имя</label>
                    <input 
                        type="text" 
                        id="contact-name" 
                        name="name" 
                        required 
                        aria-describedby="contact-name-error"
                    >
                    <div id="contact-name-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group">
                    <label for="contact-email">Email</label>
                    <input 
                        type="email" 
                        id="contact-email" 
                        name="email" 
                        required 
                        aria-describedby="contact-email-error"
                    >
                    <div id="contact-email-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group">
                    <label for="contact-subject">Тема сообщения</label>
                    <select id="contact-subject" name="subject" required aria-describedby="contact-subject-error">
                        <option value="">Выберите тему</option>
                        <option value="general">Общий вопрос</option>
                        <option value="technical">Техническая поддержка</option>
                        <option value="complaint">Жалоба</option>
                        <option value="suggestion">Предложение</option>
                        <option value="other">Другое</option>
                    </select>
                    <div id="contact-subject-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group">
                    <label for="contact-message">Сообщение</label>
                    <textarea 
                        id="contact-message" 
                        name="message" 
                        rows="6" 
                        required 
                        minlength="10" 
                        maxlength="1000"
                        aria-describedby="contact-message-error"
                    ></textarea>
                    <div id="contact-message-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <div class="form-group">
                    <label for="contact-attachment">Прикрепить файл (необязательно)</label>
                    <input 
                        type="file" 
                        id="contact-attachment" 
                        name="attachment" 
                        accept=".pdf,.doc,.docx,.jpg,.jpeg,.png"
                    >
                </div>
                
                <div class="form-group checkbox-group">
                    <input 
                        type="checkbox" 
                        id="contact-agreement" 
                        name="agreement" 
                        required
                        aria-describedby="contact-agreement-error"
                    >
                    <label for="contact-agreement">Я согласен на обработку персональных данных</label>
                    <div id="contact-agreement-error" class="error-message" role="alert" aria-live="polite"></div>
                </div>
                
                <button type="submit" class="btn btn-primary">Отправить сообщение</button>
            </form>
        </div>
    </main>

    <script>
        document.getElementById('contact-form').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Сброс ошибок
            document.querySelectorAll('.error-message').forEach(el => el.textContent = '');
            
            // Получение значений полей
            const name = document.getElementById('contact-name').value.trim();
            const email = document.getElementById('contact-email').value.trim();
            const subject = document.getElementById('contact-subject').value;
            const message = document.getElementById('contact-message').value.trim();
            const agreement = document.getElementById('contact-agreement').checked;
            
            let isValid = true;
            
            // Валидация имени
            if (!name) {
                document.getElementById('contact-name-error').textContent = 'Пожалуйста, введите имя';
                isValid = false;
            }
            
            // Валидация email
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!email) {
                document.getElementById('contact-email-error').textContent = 'Пожалуйста, введите email';
                isValid = false;
            } else if (!emailRegex.test(email)) {
                document.getElementById('contact-email-error').textContent = 'Пожалуйста, введите корректный email';
                isValid = false;
            }
            
            // Валидация темы
            if (!subject) {
                document.getElementById('contact-subject-error').textContent = 'Пожалуйста, выберите тему';
                isValid = false;
            }
            
            // Валидация сообщения
            if (!message) {
                document.getElementById('contact-message-error').textContent = 'Пожалуйста, введите сообщение';
                isValid = false;
            } else if (message.length < 10) {
                document.getElementById('contact-message-error').textContent = 'Сообщение должно содержать не менее 10 символов';
                isValid = false;
            }
            
            // Валидация согласия
            if (!agreement) {
                document.getElementById('contact-agreement-error').textContent = 'Необходимо согласиться на обработку персональных данных';
                isValid = false;
            }
            
            // Если все валидации пройдены, отправляем форму
            if (isValid) {
                console.log('Форма обратной связи валидна, отправляем данные...');
                // this.submit(); // Раскомментируйте для реальной отправки
            }
        });
    </script>
</body>
</html>
```

## Форма поиска

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Поиск - Название сайта</title>
    <link rel="stylesheet" href="styles/forms.css">
</head>
<body>
    <main>
        <div class="form-container">
            <h1>Поиск по сайту</h1>
            
            <form id="search-form" action="/search" method="GET">
                <div class="search-form-group">
                    <label for="search-query" class="sr-only">Поиск</label>
                    <input 
                        type="search" 
                        id="search-query" 
                        name="q" 
                        placeholder="Введите поисковый запрос..." 
                        required
                        autocomplete="off"
                        aria-describedby="search-results"
                    >
                    <button type="submit" class="search-btn">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                            <path d="M11 17.5C14.5899 17.5 17.5 14.5899 17.5 11C17.5 7.41015 14.5899 4.5 11 4.5C7.41015 4.5 4.5 7.41015 4.5 11C4.5 14.5899 7.41015 17.5 11 17.5Z" stroke="currentColor" stroke-width="2"/>
                            <path d="M19.5 19.5L15 15" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                        </svg>
                        <span class="sr-only">Поиск</span>
                    </button>
                </div>
                
                <div class="search-options">
                    <div class="form-group checkbox-group">
                        <input type="checkbox" id="search-title" name="in_title" checked>
                        <label for="search-title">Искать в заголовках</label>
                    </div>
                    
                    <div class="form-group checkbox-group">
                        <input type="checkbox" id="search-content" name="in_content" checked>
                        <label for="search-content">Искать в содержимом</label>
                    </div>
                    
                    <div class="form-group checkbox-group">
                        <input type="checkbox" id="search-tags" name="in_tags">
                        <label for="search-tags">Искать в тегах</label>
                    </div>
                </div>
            </form>
            
            <div id="search-results" class="search-results" role="region" aria-live="polite">
                <!-- Результаты поиска будут отображаться здесь -->
            </div>
        </div>
    </main>

    <script>
        // Пример функциональности поиска
        document.getElementById('search-form').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const query = document.getElementById('search-query').value.trim();
            
            if (query) {
                console.log('Выполняем поиск по запросу:', query);
                // Здесь можно реализовать AJAX-запрос для поиска
                // или перенаправление на страницу результатов
            }
        });
        
        // Добавляем автодополнение (простой пример)
        const searchInput = document.getElementById('search-query');
        const suggestions = [
            'HTML шаблоны',
            'CSS стили',
            'JavaScript',
            'Веб-разработка',
            'Доступность'
        ];
        
        searchInput.addEventListener('input', function() {
            const value = this.value.toLowerCase();
            if (value.length > 2) {
                // Здесь можно реализовать логику автодополнения
                console.log('Показываем автодополнение для:', value);
            }
        });
    </script>
</body>
</html>
```

## Форма заказа

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Оформление заказа - Название сайта</title>
    <link rel="stylesheet" href="styles/forms.css">
</head>
<body>
    <main>
        <div class="form-container">
            <h1>Оформление заказа</h1>
            
            <form id="order-form" action="/checkout" method="POST">
                <div class="form-section">
                    <h2>Контактная информация</h2>
                    
                    <div class="form-group">
                        <label for="order-name">ФИО</label>
                        <input 
                            type="text" 
                            id="order-name" 
                            name="full_name" 
                            required 
                            aria-describedby="order-name-error"
                        >
                        <div id="order-name-error" class="error-message" role="alert" aria-live="polite"></div>
                    </div>
                    
                    <div class="form-group">
                        <label for="order-email">Email</label>
                        <input 
                            type="email" 
                            id="order-email" 
                            name="email" 
                            required 
                            aria-describedby="order-email-error"
                        >
                        <div id="order-email-error" class="error-message" role="alert" aria-live="polite"></div>
                    </div>
                    
                    <div class="form-group">
                        <label for="order-phone">Телефон</label>
                        <input 
                            type="tel" 
                            id="order-phone" 
                            name="phone" 
                            required 
                            aria-describedby="order-phone-error"
                        >
                        <div id="order-phone-error" class="error-message" role="alert" aria-live="polite"></div>
                    </div>
                </div>
                
                <div class="form-section">
                    <h2>Адрес доставки</h2>
                    
                    <div class="form-group">
                        <label for="order-country">Страна</label>
                        <select id="order-country" name="country" required aria-describedby="order-country-error">
                            <option value="">Выберите страну</option>
                            <option value="ru">Россия</option>
                            <option value="kz">Казахстан</option>
                            <option value="by">Беларусь</option>
                        </select>
                        <div id="order-country-error" class="error-message" role="alert" aria-live="polite"></div>
                    </div>
                    
                    <div class="form-group">
                        <label for="order-city">Город</label>
                        <input 
                            type="text" 
                            id="order-city" 
                            name="city" 
                            required 
                            aria-describedby="order-city-error"
                        >
                        <div id="order-city-error" class="error-message" role="alert" aria-live="polite"></div>
                    </div>
                    
                    <div class="form-group">
                        <label for="order-address">Адрес</label>
                        <input 
                            type="text" 
                            id="order-address" 
                            name="address" 
                            required 
                            aria-describedby="order-address-error"
                        >
                        <div id="order-address-error" class="error-message" role="alert" aria-live="polite"></div>
                    </div>
                    
                    <div class="form-group">
                        <label for="order-postcode">Почтовый индекс</label>
                        <input 
                            type="text" 
                            id="order-postcode" 
                            name="postcode" 
                            required 
                            pattern="[0-9]{6}"
                            aria-describedby="order-postcode-error"
                        >
                        <div id="order-postcode-error" class="error-message" role="alert" aria-live="polite"></div>
                    </div>
                </div>
                
                <div class="form-section">
                    <h2>Способ доставки</h2>
                    
                    <div class="form-group radio-group">
                        <input type="radio" id="delivery-standard" name="delivery_type" value="standard" checked>
                        <label for="delivery-standard">Стандартная доставка (3-5 дней)</label>
                    </div>
                    
                    <div class="form-group radio-group">
                        <input type="radio" id="delivery-express" name="delivery_type" value="express">
                        <label for="delivery-express">Экспресс доставка (1-2 дня)</label>
                    </div>
                    
                    <div class="form-group radio-group">
                        <input type="radio" id="delivery-pickup" name="delivery_type" value="pickup">
                        <label for="delivery-pickup">Самовывоз из магазина</label>
                    </div>
                </div>
                
                <div class="form-section">
                    <h2>Способ оплаты</h2>
                    
                    <div class="form-group radio-group">
                        <input type="radio" id="payment-card" name="payment_method" value="card" checked>
                        <label for="payment-card">Банковской картой</label>
                    </div>
                    
                    <div class="form-group radio-group">
                        <input type="radio" id="payment-cash" name="payment_method" value="cash">
                        <label for="payment-cash">Наличными при получении</label>
                    </div>
                    
                    <div class="form-group radio-group">
                        <input type="radio" id="payment-online" name="payment_method" value="online">
                        <label for="payment-online">Онлайн-платеж</label>
                    </div>
                </div>
                
                <div class="form-section">
                    <h2>Комментарии к заказу</h2>
                    
                    <div class="form-group">
                        <label for="order-comments">Дополнительная информация</label>
                        <textarea 
                            id="order-comments" 
                            name="comments" 
                            rows="4" 
                            maxlength="500"
                            placeholder="Укажите особые пожелания по доставке или упаковке"
                        ></textarea>
                    </div>
                </div>
                
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Оформить заказ</button>
                    <a href="/cart" class="btn btn-secondary">Назад в корзину</a>
                </div>
            </form>
        </div>
    </main>

    <script>
        document.getElementById('order-form').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Сброс ошибок
            document.querySelectorAll('.error-message').forEach(el => el.textContent = '');
            
            // Получение значений полей
            const fullName = document.getElementById('order-name').value.trim();
            const email = document.getElementById('order-email').value.trim();
            const phone = document.getElementById('order-phone').value.trim();
            const country = document.getElementById('order-country').value;
            const city = document.getElementById('order-city').value.trim();
            const address = document.getElementById('order-address').value.trim();
            const postcode = document.getElementById('order-postcode').value.trim();
            
            let isValid = true;
            
            // Валидация ФИО
            if (!fullName) {
                document.getElementById('order-name-error').textContent = 'Пожалуйста, введите ФИО';
                isValid = false;
            }
            
            // Валидация email
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!email) {
                document.getElementById('order-email-error').textContent = 'Пожалуйста, введите email';
                isValid = false;
            } else if (!emailRegex.test(email)) {
                document.getElementById('order-email-error').textContent = 'Пожалуйста, введите корректный email';
                isValid = false;
            }
            
            // Валидация телефона
            if (!phone) {
                document.getElementById('order-phone-error').textContent = 'Пожалуйста, введите телефон';
                isValid = false;
            }
            
            // Валидация страны
            if (!country) {
                document.getElementById('order-country-error').textContent = 'Пожалуйста, выберите страну';
                isValid = false;
            }
            
            // Валидация города
            if (!city) {
                document.getElementById('order-city-error').textContent = 'Пожалуйста, введите город';
                isValid = false;
            }
            
            // Валидация адреса
            if (!address) {
                document.getElementById('order-address-error').textContent = 'Пожалуйста, введите адрес';
                isValid = false;
            }
            
            // Валидация почтового индекса
            const postcodeRegex = /^[0-9]{6}$/;
            if (!postcode) {
                document.getElementById('order-postcode-error').textContent = 'Пожалуйста, введите почтовый индекс';
                isValid = false;
            } else if (!postcodeRegex.test(postcode)) {
                document.getElementById('order-postcode-error').textContent = 'Почтовый индекс должен содержать 6 цифр';
                isValid = false;
            }
            
            // Если все валидации пройдены, отправляем форму
            if (isValid) {
                console.log('Форма заказа валидна, отправляем данные...');
                // this.submit(); // Раскомментируйте для реальной отправки
            }
        });
    </script>
</body>
</html>
```

## Практические рекомендации

1. **Валидация данных**: Обязательно реализуйте как клиентскую, так и серверную валидацию данных формы.

2. **Доступность**: Используйте правильные ARIA-атрибуты и семантические элементы для обеспечения доступности.

3. **Безопасность**: Защитите формы от CSRF-атак, используйте HTTPS и фильтруйте вводимые данные.

4. **UX-дизайн**: Обеспечьте понятную навигацию по форме, используйте подсказки и валидацию в реальном времени.

5. **Российские требования**: Обязательно включайте согласие на обработку персональных данных.

6. **Мобильная адаптация**: Убедитесь, что формы корректно отображаются и работают на мобильных устройствах.

## Заключение

HTML-формы являются важным элементом взаимодействия между пользователем и веб-сайтом. Правильно спроектированные формы обеспечивают удобство использования, безопасность данных и высокую конверсию. В российских реалиях 2025 года особенно важно учитывать требования законодательства и обеспечивать максимальную доступность форм для всех пользователей.

См. также:
- [[Базовые-шаблоны]]
- [[Шаблоны-для-доступности]]
- [[Шаблоны-и-производительность]]