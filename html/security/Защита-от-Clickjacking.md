---
aliases: ["Clickjacking Protection", "Защита от кликджекинга", "X-Frame-Options", "Frame Busting", "Кликджекинг"]
tags: [html/security, web-security, clickjacking, x-frame-options, security-best-practices, frontend-security]
---

# Защита от Clickjacking (Кликджекинга)

## Обзор

**Clickjacking** (кликджекинг) - это тип атаки на веб-приложения, при котором пользователь обманом заставляется кликнуть на элементы, которые он не видит или не осознает. Атакующий помещает целевой веб-сайт в прозрачный iframe поверх своих элементов управления, создавая видимость легитимного интерфейса, в то время как пользователь на самом деле взаимодействует с защищаемым сайтом без его ведома.

В российской реальности 2025 года защита от кликджекинга особенно важна в свете новых требований законодательства о защите персональных данных и кибербезопасности, а также ужесточения норм веб-безопасности в соответствии с международными стандартами.

## Как работает Clickjacking

### Принцип атаки

```html
<style>
   iframe {
     position: absolute;
     top: 0;
     left: 0;
     width: 100%;
     height: 100%;
     opacity: 0.01; /* Почти прозрачный */
     z-index: 1000;
   }
   .fake-button {
     position: relative;
     z-index: 999;
     background: #4CAF50;
     color: white;
     padding: 10px 20px;
     cursor: pointer;
     text-align: center;
     display: inline-block;
   }
</style>

<div>
   <p>Нажмите на кнопку "Подарок"!</p>
   <div class="fake-button">🎁 Подарок</div>
   <iframe src="https://target-site.com/settings"></iframe>
</div>
```

В этом примере пользователь видит привлекательную кнопку "Подарок", но при клике он на самом деле взаимодействует с настройками на `target-site.com`, возможно, неосознанно изменяя важные настройки или выполняет действия, которые атакующий хотел.

### Типы атак

- **Клик-джекинг с одним кликом** - пользователь совершает одно действие, например, нажимает кнопку
- **Многошаговый клик-джекинг** - пользователь проходит несколько этапов, думая, что он на другом сайте
- **Текст-джекинг** - пользователь вводит текст в форму, которая перенаправляется на другой сайт
- **Клик-джекинг с видеокамерой** - вредоносный сайт получает доступ к камере через фрейм

## Методы защиты

### 1. Заголовок X-Frame-Options

Самый распространенный и эффективный способ защиты - использование HTTP-заголовка `X-Frame-Options`:

```http
X-Frame-Options: DENY
X-Frame-Options: SAMEORIGIN
X-Frame-Options: ALLOW-FROM https://example.com/
```

- `DENY` - запрещает отображение страницы во фрейме на любом сайте
- `SAMEORIGIN` - разрешает отображение только в фреймах того же домена
- `ALLOW-FROM uri` - разрешает отображение только на указанных доменах (устаревший, не поддерживается во всех браузерах)

### 2. Content Security Policy (CSP)

Современный подход - использование CSP с директивой `frame-ancestors`:

```http
Content-Security-Policy: frame-ancestors 'none';
Content-Security-Policy: frame-ancestors 'self';
Content-Security-Policy: frame-ancestors example.com *.example.com;
```

Эта директива более гибкая и позволяет указывать разрешенные домены для встраивания контента.

### 3. Frame Busting (JavaScript-защита)

```javascript
if (window.location !== window.top.location) {
   window.top.location = window.location;
}
```

> [!warning] Важно
> Frame busting может быть обойден в современных браузерах, особенно с использованием `sandbox` атрибута в iframe. CSP с `frame-ancestors` более надежен.

### 4. Double-Submit Cookie Pattern

Для критических действий можно использовать проверку на то, что запрос действительно инициирован пользователем:

```javascript
// Установка токена в cookie
document.cookie = "csrf-token=" + generateCSRFToken();

// Проверка токена на сервере
function validateCSRFToken(token) {
   // Проверка токена на сервере
}
```

## Практические рекомендации

### Для разработчиков

1. **Всегда используйте X-Frame-Options или CSP** для страниц, которые не должны быть встроены в iframe
2. **Тестирование на кликджекинг** - регулярно проверяйте приложение с использованием инструментов типа OWASP ZAP или Burp Suite
3. **Критические функции** - для важных действий (например, смена пароля) используйте дополнительные проверки и подтверждения
4. **Обновление зависимостей** - следите за безопасностью используемых библиотек и фреймворков

### Для администраторов серверов

```nginx
# Пример для nginx
add_header X-Frame-Options "SAMEORIGIN";
add_header Content-Security-Policy "frame-ancestors 'self'";
```

```apache
# Пример для Apache
Header always append X-Frame-Options SAMEORIGIN
Header always set Content-Security-Policy "frame-ancestors 'self'"
```

### Российские особенности

В 2025 году в России действуют ужесточенные требования к защите персональных данных в соответствии с 152-ФЗ "О персональных данных". Защита от кликджекинга особенно важна для:

- **Государственных сервисов** - порталы государственных услуг, электронное голосование
- **Финансовых сервисов** - банки, платежные системы, инвестиционные платформы
- **Медицинских систем** - электронные медицинские карты, телемедицина
- **Образовательных платформ** - системы дистанционного обучения, электронные дневники

> [!tip] Совет
> В соответствии с российским законодательством о защите персональных данных, утечка данных через кликджекинг может повлечь административную и уголовную ответственность. Регулярно проводите аудит безопасности веб-приложений.

## Тестирование защиты

### Проверка X-Frame-Options

```bash
curl -I https://yoursite.com/sensitive-page
# Должен содержать: X-Frame-Options: DENY или SAMEORIGIN
```

### Проверка CSP

```bash
curl -I https://yoursite.com/
# Должен содержать: Content-Security-Policy: frame-ancestors 'self' или 'none'
```

### Ручное тестирование

Создайте HTML-страницу с iframe, содержащим ваш сайт:

```html
<!DOCTYPE html>
<html>
<head>
   <title>Тест кликджекинга</title>
</head>
<body>
   <iframe src="https://yoursite.com/" width="100%" height="100%"></iframe>
</body>
</html>
```

Если сайт защищен, iframe не должен загружаться или должен отображаться пустым.

## Современные угрозы и эволюция атак

В 2025 году атаки кликджекинга адаптируются к новым технологиям:

- **SPA-приложения** - атаки на одностраничные приложения с динамической загрузкой
- **Мобильные веб-приложения** - кликджекинг в веб-вью мобильных приложений
- **Социальные инженеры** - сочетание кликджекинга с фишингом и социальной инженерией

## Связанные темы

- [[CSRF-защита]] - защита от подделки межсайтовых запросов
- [[XSS-атаки]] - межсайтовый скриптинг
- [[Content-Security-Policy]] - политика безопасности контента
- [[HTTP-Security-Headers]] - важные заголовки безопасности
- [[OWASP-Top-10]] - топ-10 уязвимостей по версии OWASP
- [[Сертификаты-SSL-TLS]] - шифрование трафика
- [[Аутентификация-и-авторизация]] - безопасная идентификация пользователей
- [[Сканеры-безопасности]] - инструменты для поиска уязвимостей

## Заключение

Защита от кликджекинга - важная часть комплексной безопасности веб-приложений. В условиях современных угроз и ужесточения требований к защите данных в России, особенно важно внедрять надежные меры защиты, включая заголовки безопасности, CSP и регулярное тестирование приложений.

Рекомендуется использовать комбинацию `X-Frame-Options` и `Content-Security-Policy` для максимальной совместимости с различными браузерами и уровнем защиты.

## Ключевые выводы

- Кликджекинг - это атака, при которой пользователь обманом кликает на элементы на встроенном сайте
- Используйте `X-Frame-Options` или `Content-Security-Policy` с директивой `frame-ancestors`
- Регулярно тестируйте приложения на наличие уязвимостей
- В России защита особенно важна из-за требований 152-ФЗ и других нормативных актов
- Совмещайте технические меры с регулярным аудитом безопасности